#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
PLATFORM_ROOT="$(cd -- "$SCRIPT_DIR/../.." && pwd)"

exec "$PLATFORM_ROOT/.venv/bin/python" -m deploy.scripts.migration_executor "$@"
