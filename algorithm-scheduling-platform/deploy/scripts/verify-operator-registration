#!/usr/bin/env bash
set -euo pipefail

platform_root="$(cd "$(dirname "$0")/../.." && pwd)"
exec "${platform_root}/.venv/bin/python" \
  "${platform_root}/deploy/scripts/verify_operator_registration.py" "$@"
