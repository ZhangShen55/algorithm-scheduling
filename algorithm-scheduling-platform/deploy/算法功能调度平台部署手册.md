# 算法功能调度平台部署手册

> 适用拓扑：Milestone 2B 七算子三 GPU 单机拓扑<br>
> 配置 schema：当前仓库 11 份 `config.toml` 与 `deploy/operator-topology.json`<br>
> 最后静态验证日期：2026-08-23<br>
> 最终 Git SHA/release：须在 `192.168.29.11` 完成 11.x 发布门禁后回填；本文不把旧历史拓扑
> release 或本地测试冒充当前验收证据。

本文是首次部署、常驻启停、升级、回滚和精确清理的唯一中文操作入口。总体设计、A 服务
字段合同和 Canonical 验收证据分别由现有设计文档、A 服务对接指南和 Harness 管理；本文
只调用仓库中的权威脚本与 Compose 文件，不复制脚本内部实现。

| 生命周期动作 | 权威入口 |
|---|---|
| 常驻启动 | `deploy/scripts/start-production-stack` |
| 状态核验 | `deploy/scripts/status-production-stack` |
| 受控停止 | `deploy/scripts/stop-production-stack` |
| 数据库迁移 | `deploy/scripts/apply-database-migrations` |
| 镜像盘点/计划/执行/重验 | `deploy/scripts/production-image-lifecycle` |

## 1. 当前交付边界

当前平台包含七类算子、21 个算子实例、18 个 GPU 实例、3 个 CPU PPT Slice 实例、四个
平台服务和四个中间件。`text_analysis/` 是保留的非平台项目，不构建、不启动、不注册、
不路由、不租约、不 Smoke。

| 资源 | GPU 0 | GPU 1 | GPU 2 | CPU |
|---|---|---|---|---|
| ASR Offline | `asr-offline-gpu0` | `asr-offline-gpu1` | `asr-offline-gpu2` | — |
| ASR Online | `asr-online-gpu0` | `asr-online-gpu1` | `asr-online-gpu2` | — |
| OCR | `ocr-gpu0` | `ocr-gpu1` | `ocr-gpu2` | — |
| VBas | `vbas-gpu0` | `vbas-gpu1` | `vbas-gpu2` | — |
| FaceRec | `facerec-gpu0` | `facerec-gpu1` | `facerec-gpu2` | — |
| ScreenDet | `screen-det-gpu0` | `screen-det-gpu1` | `screen-det-gpu2` | — |
| PPT Slice | — | — | — | `ppt-slice-cpu0`、`ppt-slice-cpu1`、`ppt-slice-cpu2` |

四个平台服务是 `control-service`、`orchestrator-service`、
`vision-orchestrator-service`、`online-gateway-service`；四个中间件是 PostgreSQL、Kafka、
Redis、MongoDB。容器共同使用外部命名网络 `algorithm-platform`。

### 1.1 端口与网络

| 组件 | 宿主机端口 | 绑定 | 容器内地址 |
|---|---:|---|---|
| PostgreSQL | 5432 | `127.0.0.1` | `postgres:5432` |
| Kafka EXTERNAL | 9092 | `127.0.0.1` | 容器使用 `kafka:29092` |
| Redis | 6379 | `127.0.0.1` | `redis:6379` |
| MongoDB | 27017 | `127.0.0.1` | `mongodb:27017` |
| control-service | 18100 | `0.0.0.0`，A 服务北向 | `control-service:18100` |
| orchestrator-service | 18101 | `127.0.0.1` | `orchestrator-service:18101` |
| vision-orchestrator-service | 18102 | `127.0.0.1` | `vision-orchestrator-service:8010` |
| online-gateway-service | 18103 | `0.0.0.0`，A 服务北向 | `online-gateway-service:8001` |
| ASR Offline GPU0/1/2 | 18083/28083/38083 | `127.0.0.1` | 8083 |
| ASR Online GPU0/1/2 | 18084/28084/38084 | `127.0.0.1` | 8084 |
| FaceRec GPU0/1/2 | 18003/28003/38003 | `127.0.0.1` | 8000 |
| OCR GPU0/1/2 | 18866/28866/38866 | `127.0.0.1` | 8866 |
| VBas GPU0/1/2 | 18981/28981/38981 | `127.0.0.1` | 8981 |
| ScreenDet GPU0/1/2 | 18880/28880/38880 | `127.0.0.1` | 8880 |
| PPT Slice CPU0/1/2 | 19001/29001/39001 | `127.0.0.1` | 9001 |

Kafka 的双 listener 固定广播
`EXTERNAL://127.0.0.1:9092` 和 `INTERNAL://kafka:29092`。A/远程主机只访问 18100 和
18103；不得把中间件、内部平台端口或 21 个算子端口开放到远程网络。

### 1.2 数据、模型和日志

- `/data/course/{task_id}` 是临时媒体目录，只能在任务终态、结果已落盘且目标边界校验后
  受控清理。
- `/data/result/{task_id}` 是默认保留的持久结果目录；常规停止、升级、镜像清理和排障
  均不得删除。
- PostgreSQL 是任务、节点、结果、Outbox 与审计事实；Kafka、Redis 不能替代数据库备份。
- MongoDB 保存人物事实和 embedding。`save_person_photo=false` 时不得在 MongoDB、日志、
  容器或持久目录保存人脸原图。
- 模型目录和外部 `model-assets.manifest.json` 是受控输入。manifest 内容、课程媒体、
  人脸原图、模型密钥、Deploy Key/私钥不得进入 Git、普通报告或镜像上下文。
- 11 类运行项目写 `logs/{instance_id}/application.log` 和 stdout，活动文件上限 100 MiB，
  归档保留 7 日。需要跨容器重建留存时才加载 `deploy/docker-compose.logs.yml`。

## 2. 服务器前置检查

固定服务器登录合同为 `root@192.168.29.11:22`，批准记录的密码为 `kedacom_123`，代码目录
为 `/root/workspace/algorithm-scheduling`。该批准例外不扩展到私钥、模型密钥或媒体。

所有命令除特别说明外均在
`/root/workspace/algorithm-scheduling/algorithm-scheduling-platform` 执行。失败时停止当前
步骤，保存 stdout/stderr 和当前 release 证据，不越过门禁继续启动或删除。

```bash
uname -m
docker version
docker compose version
nvidia-smi --query-gpu=index,uuid,memory.total --format=csv,noheader
df -h / /data/course /data/result
date --iso-8601=seconds
git -C .. status --short
```

判定：架构为 `x86_64`；Docker/Compose v2 和 NVIDIA Container Runtime 可用；恰有三张
GPU；时钟已同步；Git SHA 完整且用于正式发布的工作树 clean。剩余空间低于 15% 或
150 GiB 时不进入更高负载/构建阶梯，低于 10% 或 100 GiB 时停止新负载并受控排空。

本里程碑不创建也不读取 `.env`。`.venv` 是平台/Harness Python 环境，不是部署配置：

```bash
export OPERATOR_REGISTRY_TOKEN="${OPERATOR_REGISTRY_TOKEN:?required}"
export EXPECTED_GIT_SHA="$(git -C .. rev-parse HEAD)"

python3 -m venv "$PWD/.venv"
"$PWD/.venv/bin/python" -m pip install '.[dev]'
"$PWD/.venv/bin/python" -c 'import aiokafka,httpx,websockets,yaml'
deploy/scripts/preflight host
```

`preflight host` 成功标记、三 GPU、精确端口占用授权、目录和 clean Git 任一失败均不得继续。

## 3. Git、镜像和模型准备

```bash
export RELEASE_TAG=v1.0_260812
export MODEL_ASSET_SOURCE=/root/workspace/.algorithm-scheduling-assets/v1.0_260812
export REPORT_ROOT="$PWD/deploy/reports"
export RELEASE_ROOT="$REPORT_ROOT/milestone-2b/releases/$RELEASE_TAG/$EXPECTED_GIT_SHA"
export PRODUCTION_ROOT="$RELEASE_ROOT/production"
export PRODUCTION_LEDGER="$PRODUCTION_ROOT/production-stack.json"

test "$(printf %s "$EXPECTED_GIT_SHA" | wc -c)" -eq 40
deploy/scripts/stage-model-assets --source "$MODEL_ASSET_SOURCE" --workspace "$PWD/.."
deploy/scripts/verify-model-assets --source "$MODEL_ASSET_SOURCE" --workspace "$PWD/.."
deploy/scripts/verify-operator-build-contexts
```

`PRODUCTION_LEDGER` 是常驻入口按 release tag 与完整 Git SHA 默认解析的 `0600` 权威账本；
start、status、stop 必须复用同一 `REPORT_ROOT`，不得让生命周期命令落入不同的默认报告根。

只使用交付方提供且权限为 `0600` 的外部 manifest；部署阶段不得调用生成器覆盖可信基线。
七个算子镜像由 `deploy/operator-images.tsv` 和 `deploy/scripts/build-images` 构建，四个平台
镜像由权威 Compose 构建，共 11 个发布目标镜像：

```bash
deploy/scripts/build-images "$RELEASE_TAG"
docker compose -f deploy/docker-compose.platform.yml build \
  control-service orchestrator-service vision-orchestrator-service online-gateway-service
```

预期：每个目标镜像都是 `linux/amd64`，且
`org.opencontainers.image.revision=$EXPECTED_GIT_SHA`。下载 Torch/CUDA wheel 时，以网络字节、
Docker cache 和磁盘写入判断是否仍在前进；不能因日志暂时安静而取消正在推进的构建。

## 4. 十一份配置权威

部署前逐项复核实际文件；不得另做 `.env` 或复制第二套容器内配置。

| 项目 | 权威路径 | 重点 |
|---|---|---|
| Control | `../control_service/config.toml` | PostgreSQL、Redis、注册/租约、目录 |
| Orchestrator | `../orchestrator_service/config.toml` | PostgreSQL、Kafka、Control、媒体目录 |
| Vision Orchestrator | `../vision_orchestrator_service/config.toml` | Kafka、Control、抽帧与聚合 |
| Online Gateway | `../online_gateway_service/config.toml` | Control、FaceRec 管理实例、HTTP/WS 池 |
| ASR Offline | `deploy/config/operators/asr_offline.gpu.toml` | 模型、并发、注册 |
| ASR Online | `deploy/config/operators/asr_online.gpu.toml` | 模型、实时会话、注册 |
| FaceRec | `deploy/config/operators/facerec.gpu.toml` | MongoDB、`save_person_photo=false` |
| OCR | `deploy/config/operators/ocr.gpu.toml` | 模型 manifest、并发 |
| PPT Slice | `deploy/config/operators/ppt_slice.cpu.toml` | 共享 PPT 路径、回调 |
| ScreenDet | `deploy/config/operators/screen_det.gpu.toml` | 模型、注册 |
| VBas | `deploy/config/operators/vbas.gpu.toml` | 帧推理、注册；不含课程级编排 |

Online Gateway 的 Milestone 2B 权威值为 `max_connections=2048`、
`max_keepalive_connections=512` 和有界 `pool_timeout_seconds=5.0`。连接池只承接请求，不是
业务容量声明：无租约时应快速返回 `50301`，不得把连接池超时产生的 `50000` 当作算子过载。

FaceRec 的新增、批量新增、查询、搜索和删除固定转发到 `facerec-gpu0` 单一管理实例；只有
`/face/recognize` 通过租约路由到三个识别实例，三实例共享 MongoDB。容量报告必须分别写
单管理实例吞吐、三识别实例吞吐和一致性。

## 5. 目录、权限和媒体源基线

```bash
install -d -m 0750 /data/course /data/result
install -d -m 0700 "$REPORT_ROOT"
test -w /data/course
test -w /data/result
```

需要宿主机持久日志时，另建 `/data/logs/algorithm-scheduling`，并通过
`LOG_ROOT=/data/logs/algorithm-scheduling deploy/scripts/preflight host` 校验；每实例使用独立
子目录。数据库卷由 Compose 管理，不用普通目录递归清理。

离线单泳道/长课压力前，从 192.168.29.11 对媒体源执行 1/3/10/30 并发下载基线。下列命令
只写 Git 外 `0600` 原始指标，不把媒体写入仓库；`MEDIA_URLS_FILE` 每行是受控 T/S/P URL：

```bash
MEDIA_URLS_FILE=/root/workspace/.algorithm-scheduling-fixtures/media-urls.txt
BASELINE_ROOT=/root/workspace/.algorithm-scheduling-restricted-reports/media-baseline
install -d -m 0700 "$BASELINE_ROOT"
for CONCURRENCY in 1 3 10 30
do
  xargs -P "$CONCURRENCY" -I '{}' curl -fsS -o /dev/null \
    -w '%{url_effective}\t%{size_download}\t%{time_connect}\t%{time_total}\t%{speed_download}\n' \
    '{}' < "$MEDIA_URLS_FILE" > "$BASELINE_ROOT/download-${CONCURRENCY}.tsv"
  chmod 0600 "$BASELINE_ROOT/download-${CONCURRENCY}.tsv"
done
```

同时在源端记录文件服务 CPU/内存/网络，在目标端记录入站网络。每阶梯计算单文件速度、总
吞吐、建连耗时和失败率。若源端/局域网先封顶或持续失败，端到端结果可保留，但不能据此
宣称 GPU 或算子达到最大吞吐。

## 6. 构建前精确清理

生产清理只能调用 `deploy/scripts/production-image-lifecycle`。严禁宽泛 prune、Compose
带卷销毁、按名称/glob 删除、未解析变量删除，以及触碰 volume、模型、Git、release 证据或
`/data/result`。

先原子盘点全部运行/停止容器、完整镜像 ID、多 tag、RepoDigest、Compose 身份、状态、
revision 和大小：

```bash
INVENTORY="$RELEASE_ROOT/inventory/containers-images.before.json"
PLAN="$RELEASE_ROOT/inventory/prebuild-cleanup-plan.json"
RESULT="$RELEASE_ROOT/cleanup/prebuild-cleanup-result.json"
deploy/scripts/production-image-lifecycle inventory --output "$INVENTORY"
```

从 inventory/Compose 审核并记录 11 个当前目标镜像完整 ID、回滚基线、基础镜像和显式
allowlist。`plan` 要求 11 次 `--current-image-id sha256:<64位>`；回滚、基础镜像、allowlist
分别使用对应的可重复参数，并只用完整旧 release SHA 选择候选：

```bash
deploy/scripts/production-image-lifecycle plan \
  --inventory "$INVENTORY" --output "$PLAN" --stage prebuild \
  --release-tag "$RELEASE_TAG" --git-sha "$EXPECTED_GIT_SHA" \
  --current-image-id "${CURRENT_IMAGE_ID_01:?full ID}" \
  --current-image-id "${CURRENT_IMAGE_ID_02:?full ID}" \
  --current-image-id "${CURRENT_IMAGE_ID_03:?full ID}" \
  --current-image-id "${CURRENT_IMAGE_ID_04:?full ID}" \
  --current-image-id "${CURRENT_IMAGE_ID_05:?full ID}" \
  --current-image-id "${CURRENT_IMAGE_ID_06:?full ID}" \
  --current-image-id "${CURRENT_IMAGE_ID_07:?full ID}" \
  --current-image-id "${CURRENT_IMAGE_ID_08:?full ID}" \
  --current-image-id "${CURRENT_IMAGE_ID_09:?full ID}" \
  --current-image-id "${CURRENT_IMAGE_ID_10:?full ID}" \
  --current-image-id "${CURRENT_IMAGE_ID_11:?full ID}" \
  --rollback-image-id "${ROLLBACK_IMAGE_ID:?full ID}" \
  --base-image-id "${BASE_IMAGE_ID:?full ID}" \
  --retired-release-sha "${RETIRED_GIT_SHA:?full SHA}"
```

预期输出是 `cleanup-plan: DRY-RUN sha256=<摘要>`。人工检查保护理由、候选原因、release、
tag/digest、预估回收空间及保护集/候选集无交集；把输出摘要作为唯一审核值：

```bash
deploy/scripts/production-image-lifecycle execute \
  --plan "$PLAN" --approved-sha256 "${APPROVED_PLAN_SHA256:?reviewed digest}" \
  --result "$RESULT"
```

执行会二次 inventory；任何容器引用/状态/镜像漂移都在首次删除前失败。删除命令只接受计划
中的完整容器/镜像 ID，并记录删除前摘要、时间、成功/失败、删除后 inspect 和
`docker system df` 前后。任一失败都使阶段不符合，不能改写原 dry-run 掩盖失败。

## 7. 首次部署、迁移和常驻启动

权威常驻入口不同于 Canonical 验收控制器：常驻入口不会在验收末尾停止本轮新算子，也不
复用原业务 snapshot/pause/restore 事务。

数据库迁移覆盖 `migrations/0001`–`0007` 及后续连续编号文件。先看只读计划；正式迁移由
常驻启动在 PostgreSQL 健康后自动顺序执行，也可独立调用：

```bash
deploy/scripts/apply-database-migrations --git-sha "$EXPECTED_GIT_SHA" --plan
deploy/scripts/apply-database-migrations --git-sha "$EXPECTED_GIT_SHA"
```

首次、重复和中断续跑使用数据库表 `algorithm_schema_migrations` 的文件名、SHA-256 和应用
Git SHA 账本；未知版本、版本间断或已应用文件摘要变化均失败关闭。

启动完整常驻栈：

```bash
deploy/scripts/start-production-stack \
  --git-sha "$EXPECTED_GIT_SHA" --release-tag "$RELEASE_TAG" \
  --reports-root "$REPORT_ROOT"
```

入口按 PostgreSQL/Kafka/Redis/MongoDB、数据库迁移、四个平台服务、GPU0、GPU1、GPU2、
CPU profile 顺序启动；每个 profile 完成立即校验 image revision、运行包、注册和首次心跳，
最后要求完整 21/21。成功后以 `0600` 原子写 `PRODUCTION_LEDGER` 指向的 29 容器权威账本。
任一步失败保留现场并返回非零；先查询状态/日志，不能改用宽泛 Compose 操作跳过。

## 8. 状态、停止和 A 服务 Smoke

```bash
STATUS_JSON="$PRODUCTION_ROOT/production-stack-status.json"
deploy/scripts/status-production-stack \
  --git-sha "$EXPECTED_GIT_SHA" --release-tag "$RELEASE_TAG" \
  --reports-root "$REPORT_ROOT" \
  --output "$STATUS_JSON"
```

预期 `status=PASS`，汇总为四中间件、四个平台服务、21 个算子、18 GPU、3 CPU、21 注册
实例，并报告三卡归属、活跃租约、29 个宿主机端口和两个共享目录。状态入口会检查运行镜像
revision、真实 NVIDIA DeviceRequests、Compose project/service、健康状态、注册生命周期和
模型 ready；只有容器数正确不足以通过。

离线最小 Smoke 使用唯一 `task_id`，只经 18100 提交并轮询；媒体 URL 指向外部 fixture：

```bash
curl -fsS -H 'Content-Type: application/json' \
  -H "X-Trace-ID: deploy-${EXPECTED_GIT_SHA}" \
  --data '{"task_id":"deploy-smoke-ppt","task_types":["PPT"],"slides_video_path":"http://192.168.29.12:5555/course/P.mp4"}' \
  http://127.0.0.1:18100/api/course-jobs
curl -fsS http://127.0.0.1:18100/api/course-jobs/deploy-smoke-ppt
```

在线最小 Smoke 只经 18103，图片仍留在 Git 外：

```bash
deploy/scripts/run-online-gateway-smoke \
  --gateway-url http://127.0.0.1:18103 \
  --image /root/workspace/.algorithm-scheduling-fixtures/online/ocr.png \
  --release-tag "$RELEASE_TAG" --git-sha "$EXPECTED_GIT_SHA" \
  --reports-root "$REPORT_ROOT"
```

实时 ASR WebSocket、FaceRec 管理/识别及四类在线 JSON 字段按
`deploy/A服务接口与部署对接指南.md` 执行；不得直连算子补足 A 服务全链路证据。

受控停栈只读取当前 release 的 `0600` 权威账本，执行前按完整 ID 复查容器、Compose、镜像
和 revision，然后只停止这 29 个容器：

```bash
deploy/scripts/stop-production-stack \
  --git-sha "$EXPECTED_GIT_SHA" --release-tag "$RELEASE_TAG" \
  --reports-root "$REPORT_ROOT"
```

默认不删除容器、镜像、volume、`/data/result`、模型或报告。先用 `--dry-run` 查看精确 ID
计划；账本缺失、权限不是 `0600` 或身份漂移时停止入口失败关闭。

## 9. 升级、回滚和验收后退役

### 9.1 同 SHA 续跑和新 SHA 升级

同 SHA 续跑先执行 `status-production-stack`；健康则不重建。新 SHA 升级依次执行：旧栈
inventory/保护基线、构建前 dry-run、模型校验、11 镜像构建/attestation、迁移计划、常驻
启动、21/21/7/7 Smoke、Campaign 和原有 217+26+6 门禁。任一新版本门禁失败都保留旧版
容器和镜像，不允许验收后退役。

### 9.2 回滚

停止新负载并等待已接受任务/租约受控排空；使用旧 Git SHA、旧配置、外部模型 manifest 和
旧 11 镜像重新调用权威常驻入口。数据库迁移是只前进合同，回滚前须确认旧程序兼容当前
schema；不通过删除数据库卷回滚。旧镜像若已在最终退役阶段删除，则按记录的 Git SHA、
Dockerfile、基础镜像和外部 manifest 重建，历史 release 证据保持只读。

### 9.3 验收后精确退役

只有新版同 SHA 的必需预检、11 镜像、四平台、21 实例、Campaign、217 条反例、26 条压力/
恢复、6 项 B 级复核全部符合，才可建立 `postacceptance` 计划。先停止并审核每个旧容器，按
完整 ID 传 `--retire-container-id`，并传 `--acceptance-status PASS`；流程先保存其 Compose、
状态、镜像 digest/release 和删除前摘要，再删旧容器、重算镜像候选并删旧镜像。

执行结果只能是 `AWAITING_REVALIDATION`，不能直接宣布清理完成。重新运行
`status-production-stack` 和 7/7 算子 Smoke 后，用实际证据发布最终结论：

```bash
deploy/scripts/production-image-lifecycle verify \
  --result "$RESULT" --stack-status "$STATUS_JSON" \
  --smoke-evidence "$RELEASE_ROOT/smoke/asr_offline.json" \
  --smoke-evidence "$RELEASE_ROOT/smoke/asr_online.json" \
  --smoke-evidence "$RELEASE_ROOT/smoke/facerec.json" \
  --smoke-evidence "$RELEASE_ROOT/smoke/ocr.json" \
  --smoke-evidence "$RELEASE_ROOT/smoke/ppt_slice.json" \
  --smoke-evidence "$RELEASE_ROOT/smoke/screen_det.json" \
  --smoke-evidence "$RELEASE_ROOT/smoke/vbas.json" \
  --output "$RELEASE_ROOT/cleanup/cleanup-completion.json"
```

任一中间件/平台/实例、GPU 归属、注册/租约或 Smoke 不就绪时，不发布清理完成结论。

## 10. 日志、备份和日常观察

```bash
docker compose -f deploy/docker-compose.platform.yml logs --tail=200 \
  postgres kafka redis mongodb control-service orchestrator-service \
  vision-orchestrator-service online-gateway-service
curl -fsS http://127.0.0.1:18100/ops/operator-instances
curl -fsS http://127.0.0.1:18100/ops/operator-instances/snapshot
curl -fsS http://127.0.0.1:18100/ops/queues
curl -fsS http://127.0.0.1:18100/ops/storage
nvidia-smi --query-compute-apps=gpu_uuid,pid,used_memory --format=csv,noheader
```

日志不得包含 Base64、媒体字节、完整请求/响应、完整 ASR/OCR 文本、embedding 或凭据。
PostgreSQL 与 `/data/result` 应按共同批次备份；MongoDB 人物事实单独备份。Kafka/Redis 可
重建运行态，不能代替业务备份。升级前验证备份可读取，但本文不把恢复演练替换成清空卷。

## 11. 常见故障排查

| 现象 | 只读查询 | 判定依据 | 受控处置 | 禁止操作 |
|---|---|---|---|---|
| 算子未注册 | `status-production-stack`、`/ops/operator-instances`、容器日志 | 缺实例、非 ONLINE、`model_ready=false` | 检查 token/config/网络，精确重启该实例并等首次心跳 | 删除 Redis key 伪造恢复 |
| 租约不释放 | `/ops/operator-instances/snapshot`、实例 active-leases | `active_lease_count` 持续非零且工作已终态 | 停止新流量，查调用方/TTL，精确恢复相关服务 | 手工写 Redis 租约 |
| Kafka lag 不下降 | `/ops/queues`、Kafka consumer group、Orchestrator 日志 | Outbox/lag 持续增长 | 验证 `kafka:29092` 和 consumer，精确重启 Orchestrator | 清空 Kafka/PostgreSQL |
| GPU 不可见/跨卡 | `docker inspect`、`nvidia-smi` | DeviceRequests、UUID、PID 与 gpu0/1/2 不符 | 停止新负载，精确重建该 profile | 修改容器名掩盖跨卡 |
| 日志不生成 | 容器 stdout、实例日志目录权限 | handler/目录不可写或实例目录冲突 | 修复精确实例目录权限后重启实例 | 记录媒体/凭据定位 |
| 共享目录不可写 | `test -w`、mount inspect、`/ops/storage` | 容器与宿主路径不一致 | 修复明确 bind/UID，先保留结果 | 删除 `/data/result` |
| 镜像清理漂移 | inventory、dry-run、Docker inspect | 新容器引用候选或摘要变化 | 放弃旧计划，重新 inventory/审核 | 宽泛 prune 或强制删镜像 |

## 12. 可勾选验收清单

### 12.1 部署前

- [ ] x86_64、Docker Compose v2、NVIDIA Runtime、三 GPU 与时钟符合。
- [ ] clean 完整 Git SHA、release tag 和 Git 外模型 manifest 已记录。
- [ ] `/data/course`、`/data/result`、报告/可选日志目录权限与空间符合。
- [ ] 11 份 `config.toml` 已逐项复核；没有 `.env`。
- [ ] 29 个端口无未授权占用，只有 18100/18103 可远程访问。
- [ ] 已取得 1/3/10/30 媒体下载基线或明确阻断原因。
- [ ] 已生成并人工审核构建前精确清理 dry-run。

### 12.2 部署后

- [ ] 四中间件、四个平台服务健康，镜像 revision 等于同一 Git SHA。
- [ ] 21/21 注册、18/18 GPU、3/3 CPU PPT Slice 与三卡 PID/cgroup 符合。
- [ ] 7/7 算子 Smoke、A 服务 18100 离线提交/查询和 18103 在线 Smoke 通过。
- [ ] 租约申请/拒绝/释放可观测，FaceRec 管理/识别边界正确。
- [ ] `/data/result`、日志和报告实际落盘，普通证据无敏感媒体。

### 12.3 升级后

- [ ] 新 SHA 的 11 镜像、迁移账本、常驻状态、21/21 和 7/7 全部通过。
- [ ] Campaign 与原有 217 条反例、26 条压力/恢复、6 项 B 级复核同 SHA 聚合。
- [ ] 失败时旧版容器/镜像仍受保护，回滚路径和 schema 兼容性已确认。
- [ ] 只有全部门禁符合才生成验收后退役 dry-run。

### 12.4 清理后

- [ ] 每个完整容器/镜像 ID 都有删除前摘要、时间、结果和删除后二次验证。
- [ ] `docker system df` 前后证据已保存；任一失败明确标记不符合。
- [ ] 四中间件、四平台、21 实例、18 GPU、3 CPU、21 注册和 7/7 Smoke 重新通过。
- [ ] volume、模型、Git、历史 release、`/data/result` 和备份仍完整。
- [ ] `cleanup-completion.json` 由清理后证据生成，而非仅因删除命令返回零。
