## 1. 建立项目治理与 Harness 骨架

- [x] 1.1 在 `ppt_slice` 创建 `harness/README.md`、`architecture-review.md`、`change-ledger.md`、`verification.md`、`scenarios/dynamic-video-corpus.md` 和 `reports/`，说明各文件所有权、运行标识和证据引用规则
- [x] 1.2 更新 `ppt_slice/AGENTS.md`，只增加 OpenSpec、Harness、全量语料、人工复核和禁止虚假完成声明等长期边界，不记录单次阈值和运行流水
- [x] 1.3 更新 `.gitignore`，禁止并排除 MP4、远程视频副本、解码特征、切片、证据帧和联系表，同时允许提交小型 JSON/CSV/Markdown 报告摘要
- [x] 1.4 在 Harness 架构复审和变更台账中登记本 OpenSpec、现有接口兼容边界、已知样本真值、指标定义和首次基线运行计划

## 2. 发现并冻结全量课程 P 视频语料

- [x] 2.1 实现同源递归目录发现器，从配置的 `/course/` 根 URL 选择 basename 包含 `PPT` 且扩展名为 `.mp4` 的文件，规范化、去重并阻止越出允许主机和路径前缀
- [x] 2.2 为每个视频采集解码后的课程名、URL、`Content-Length`、`Last-Modified`、duration、codec、fps、分辨率和资源指纹，无法获得的元数据明确留空并记录原因
- [x] 2.3 生成带 `run_id` 和发现时间的冻结 `inventory.json`，核对首轮发现数量与提案阶段观察到的 40 个目录页、37 个 P 视频，禁止把该数量写成代码常量
- [x] 2.4 将已有真值样本固定到校准集，其余按规范化课程 URL 稳定哈希确定性划分 70% 校准集和 30% 保留验证集，确保同一课程不跨集合并记录划分规则及原因
- [x] 2.5 增加目录解析、URL 规范化、循环链接、重复链接、跨域过滤、新增视频自动纳入和失败条目保留测试

## 3. 建立当前实现基线和确定性测试输入

- [x] 3.1 使用当前实现处理已知约 55 分钟 P 视频，记录总时长、关键帧间隔、总切片数、`39:28-43:47` 内切片数与时间戳、处理耗时和峰值内存，不提交视频或生成图片
- [x] 3.2 对首轮冻结 inventory 运行现有切片基线，按视频记录切片数量、每分钟密度、异常密集区间、处理状态和性能，失败项不得静默移除
- [x] 3.3 根据全量语料关键帧间隔分布确定第一版使用关键帧观测或固定频率解码采样，并把边界精度和性能理由记录到 Harness
- [x] 3.4 构造正常换页、短动画、持续动态、短暂停顿、连续滚动、文件末尾动态和异常时间戳等确定性时序夹具，先编写失败测试覆盖状态转换与区间规则

## 4. 实现动态指标、配置与状态机

- [x] 4.1 在 `config.toml` 和配置模型增加带中文/清晰注释的启用开关、采样间隔、变化阈值、活动区域比例、动态窗口、确认时长、退出稳定、短间隙合并和候选稳定时长，并验证非法配置阻止启动
- [x] 4.2 实现独立画面活动指标，复用像素差异并增加网格活动比例，验证鼠标小范围变化不触发全局动态，而持续大范围变化可以触发
- [x] 4.3 实现由视频毫秒时间戳驱动的 `STABLE`、`DYNAMIC_CANDIDATE`、`DYNAMIC`、`STABILIZING` 状态机，支持起点回看、退出迟滞、短间隙合并和文件结束闭合
- [x] 4.4 为区间计算确定性的 `type`、`start_ms`、`end_ms`、`confidence` 和 `reason`，保证排序、互不重叠、置信度范围与相同输入配置的可重复性
- [ ] 4.5 v6 校准漏报审计已证明低运动视频会被像素/网格活动拆断；记录失败证据，并增加可关闭且“只保活、不触发”的光流特征及对应测试

## 5. 接入视频处理与切片抑制

- [x] 5.1 将采样观测和状态机接入单次视频读取流程，保持 `pts * time_base` 时间轴，并处理缺失时间戳、异常观测间隔、取消和解码失败
- [x] 5.2 将立即落盘改为有界候选切片延迟发布，只保留必要分析帧和最新候选，确保被回看为动态的候选不生成最终 JPEG 或残留 `.part`
- [x] 5.3 在动态结束后等待连续稳定，再恢复原有 `threshold` 去重并发布一张稳定页面，验证区间前后课件仍能正常切片
- [x] 5.4 保持单 Uvicorn Worker 的原子任务容量和算子内部 `max_concurrent_tasks` 并发能力、后台线程释放、失败计数与任务删除行为不变
- [x] 5.5 增加有效观测数、动态区间数、候选抑制数、发布切片数、算法版本和有效配置摘要日志，不记录敏感 URL 凭据

## 6. 扩展共享结果契约

- [x] 6.1 为共享结果增加 `DynamicSegment`，在 `manifest.json` 写入 `dynamic_segments`，保持 `schema_version=1`、既有图片字段和 `count` 语义
- [x] 6.2 在一次性终态回调中返回与 manifest 一致的 `dynamic_segments`，继续禁止 Base64 和逐图回调，并验证回调失败不造成重复发布
- [x] 6.3 增加成功、空列表、失败终态、区间排序、算法元信息和原子 manifest 发布测试，确认旧请求/响应字段、接口路径和状态码兼容

## 7. 实现全量 Runner、证据与复核工作流

- [x] 7.1 实现基于冻结 inventory 的有界并发 runner，支持超时、有限重试、按视频检查点、资源指纹校验和中断恢复，并区分缓存运行与最终端到端运行
- [x] 7.2 为每个候选直接从远程 URL 流式提取起点前后、中点和终点前后的带时间戳静态证据帧及联系表，存入 Git 忽略目录并在结果中保存稳定引用，不生成 MP4、GIF 或其他视频预览
- [x] 7.3 建立与算法原始输出分离的复核记录，支持 `CONFIRMED_VIDEO`、`CONFIRMED_SCROLL`、`FALSE_POSITIVE`、`UNCERTAIN`、复核者、时间和备注
- [x] 7.4 实现非候选区域漏报检查，覆盖固定时间网格、长时间未切片、切片异常密集和高变化未过阈值区间，为疑似漏报生成证据与待复核记录
- [x] 7.5 允许 AI 基于证据辅助初审，但无法确定时强制输出 `UNCERTAIN`，并生成所有不确定项和疑似漏报的人工复核清单

## 8. 生成指标和中文统计报告

- [x] 8.1 每轮生成 `segments.json`、`segments.csv` 和 `疑似动态区间复核报告.md`，明细包含课程、URL、时长、开始/结束、类型、置信度、证据、复核结论、备注、算法版本和配置摘要
- [x] 8.2 在中文报告单列目录发现失败、URL 不可达、探测失败、解码失败、运行失败和未复核条目，不允许从验收分母静默剔除
- [x] 8.3 计算发现覆盖率、处理完成率、复核覆盖率、segment precision/recall、时间覆盖 precision/recall、边界误差 p50/p95、每小时误报数、切片抑制数、处理速度和峰值内存
- [x] 8.4 实现完成状态判定：只有全部门槛满足且当前冻结语料无已知误报、漏报时才标记通过；任何不可访问、失败或 `UNCERTAIN` 均标记未完成或有阻塞
- [x] 8.5 报告的完成声明绑定 `run_id`、inventory 指纹、算法版本、代码提交和配置摘要，并明确不代表未来未知课程绝对百分之百准确

## 9. 校准、保留集验收与全量最终复跑

- [ ] 9.1 在校准集运行算法、生成证据并完成候选及漏报复核，根据误报漏报类型调整配置或特征；对长静止镜头造成的动态段断裂使用“连续动态簇 + 尾部候选延迟发布”修复，并在 change ledger 保存失败配置和指标变化
- [x] 9.2 验证已知视频对 `[2368000,2627000)` 覆盖至少 90%、两端误差分别不超过 20 秒、核心 `[2388000,2607000)` 无切片，且动态前后可恢复稳定切片
- [ ] 9.3 冻结算法和配置后首次揭示保留验证集，完整生成证据和复核报告；如未达门槛，以新版本和新 `run_id` 开启下一轮，不覆盖失败证据
- [ ] 9.4 在最终候选版本上重新发现并冻结全量 inventory，对所有可访问视频进行一次不依赖历史特征结果的端到端复跑并完成 100% 候选和漏报检查复核
- [ ] 9.5 持续修复全量语料中的已知误报和漏报，直到满足规范门槛；若受不可访问资源或无法判定样本阻塞，明确记录而不宣称通过

## 10. 项目验证与文档收尾

- [x] 10.1 在 `ppt_slice` Conda 环境运行 `compileall`、`from app.main import app`、全部单元/契约测试，并验证关闭动态检测后恢复旧流程
- [x] 10.2 使用本地合成视频完成真实解码测试，证明普通换页保留一张切片、持续动态不产生切片、结束稳定页恢复切片且无残留 `.part`
- [x] 10.3 启动 `app.main:app`，检查健康、版本与现有 PPT 任务接口，再执行一条真实推理任务验证共享路径、manifest 和一次终态回调
- [x] 10.4 更新 README、接口文档、`config.toml` 注释和 Harness 使用说明，写明 `dynamic_segments`、全量验收命令、证据复核流程、回滚开关和平台未持久化字段的边界
- [ ] 10.5 复审 OpenSpec、AGENTS、Harness、实现和报告一致性，运行 `openspec validate detect-ppt-video-playback-segments --strict`，把最终验证命令与结果写入 Harness
