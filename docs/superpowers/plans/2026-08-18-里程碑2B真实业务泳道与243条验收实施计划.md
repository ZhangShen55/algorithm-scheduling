# 里程碑 2B 真实业务泳道与 243 条验收实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在新不可变 release 中关闭 FaceRec 三卡证据缺口，贯通 PPT、ASR、视觉和在线真实业务泳道，并实际执行全部 217 条反例和 26 条压力用例。

**Architecture:** 保持四服务边界和现有算子合同。先建立兼容历史报告的可执行用例证据合同，再按 FaceRec、PPT/ASR、视觉、在线和完整验收五个阶段推进；平台状态只能由运行中的编排服务根据真实调用事实更新。

**Tech Stack:** Python 3.11、FastAPI、SQLAlchemy/PostgreSQL、Redis、aiokafka/Kafka、HTTPX、WebSocket、FFmpeg、Docker Compose、NVIDIA Container Runtime、Pytest、OpenSpec、Harness。

---

## 0. 当前基线和执行规则

- 工作分支：`codex/milestone-2b-three-gpu-deployment`。
- 历史证据：`v1.0_260812/7efac20cf980ee64ea78fe297af6dfdfb2df5b28`，83 通过、6 失败、243 未执行。
- FaceRec 默认探针已经在当前代码中使用 `python3`；本计划验证该修复并在新 SHA 重跑，不修改旧报告。
- 当前 OpenSpec：`close-platform-runtime-and-harness-gaps`，35/62。
- 每个代码任务按 RED -> GREEN -> 聚焦回归 -> 中文 Conventional Commit 执行。
- 远端失败先写 Harness，再继续无依赖任务；涉及数据、磁盘、GPU 或既有容器安全时停止相关动作。
- 只暂存本计划产生的文件。不得纳入用户现有未跟踪文件。

## 1. 文件职责图

### Harness 与报告

- `algorithm-scheduling-platform/deploy/milestone-2b-case-catalog.yaml`：243 条权威用例的结构化定义。
- `algorithm-scheduling-platform/scripts/milestone_2b_case_catalog.py`：严格加载、计数和阶段选择。
- `algorithm-scheduling-platform/scripts/run_milestone_2b_case_batch.py`：有界执行、失败固化和 write-once 发布。
- `algorithm-scheduling-platform/scripts/milestone_2b_case_runners/`：按部署、平台、离线、视觉、在线和压力分组的 runner。
- `algorithm-scheduling-platform/scripts/milestone_2b_report_contract.py`：兼容历史声明和新真实执行证据。
- `algorithm-scheduling-platform/scripts/aggregate_milestone_2b_cases.py`：优先聚合真实用例，缺失时保留未执行事实。
- `algorithm-scheduling-platform/scripts/render_milestone_2b_report.py`：校验原始证据归属并输出最终结论。

### 离线通用编排

- `orchestrator_service/app/application/node_adapters.py`：按节点码选择真实执行适配器。
- `orchestrator_service/app/application/executor.py`：同步节点和异步 PPT 节点生命周期。
- `orchestrator_service/app/application/vision_events.py`：视觉命令和事件状态推进。
- `orchestrator_service/app/infrastructure/media.py`：按 `submission_id` 下载协调。
- `orchestrator_service/app/infrastructure/ppt_runtime.py`：PPT 受理、续租、回调和 manifest 对账。
- 现有 `ppt_slice.py`、`ppt_text.py`、`audio.py`、`asr.py`、`course_overview.py` 保持单一适配职责。

### 视觉编排

- `vision_orchestrator_service/app/application/runtime.py`：Kafka command/event 生命周期。
- `vision_orchestrator_service/app/application/analyzer.py`：课程级抽帧、细化、聚合和证据组合。
- `vision_orchestrator_service/app/infrastructure/frame_extractor.py`：安全本地视频抽帧。
- 现有 `adaptive_scan.py`、`behavior_intervals.py`、`student_aggregation.py`、`vbas.py`、`cache.py`、`evidence.py` 保持领域或适配职责。

### 在线网关

- `online_gateway_service/app/api/routes.py`：现有三图片入口和实时 ASR，新增人物管理代理。
- `online_gateway_service/app/infrastructure/persons.py`：FaceRec 人物管理 HTTP 适配。
- `online_gateway_service/app/infrastructure/websocket_proxy.py`：会话粘性、关闭和租约释放。

## 2. 阶段一：FaceRec 证据与用例执行框架

### Task 1: 将 243 条矩阵固化为严格目录

**Files:**
- Create: `algorithm-scheduling-platform/deploy/milestone-2b-case-catalog.yaml`
- Create: `algorithm-scheduling-platform/scripts/milestone_2b_case_catalog.py`
- Create: `algorithm-scheduling-platform/tests/test_milestone_2b_case_catalog.py`
- Modify: `openspec/changes/close-platform-runtime-and-harness-gaps/tasks.md`

- [ ] **Step 1: 写目录加载失败测试**

测试必须断言总数 243、negative=217、load=26、ID 唯一，并固定前缀计数：

```python
EXPECTED_PREFIX_COUNTS = {
    "DEP": 20, "GPU": 20, "REG": 20, "INF": 16, "JOB": 20,
    "FILE": 16, "PPT": 15, "OCR": 5, "KEY": 5, "ASR": 18,
    "VIS": 28, "ONL": 20, "FACE": 14, "LOAD": 26,
}

def test_catalog_contains_exact_authority() -> None:
    catalog = load_case_catalog(CATALOG_PATH)
    assert len(catalog.cases) == 243
    assert catalog.count_by_category() == {"negative": 217, "load": 26}
    assert catalog.count_by_prefix() == EXPECTED_PREFIX_COUNTS
    assert len({case.case_id for case in catalog.cases}) == 243
```

- [ ] **Step 2: 运行测试确认 RED**

Run: `cd algorithm-scheduling-platform && .venv/bin/python -m pytest -q tests/test_milestone_2b_case_catalog.py`

Expected: FAIL，提示模块或目录文件不存在。

- [ ] **Step 3: 实现严格数据类型和加载器**

每条目录记录只允许以下字段：

```python
@dataclass(frozen=True, slots=True)
class CaseDefinition:
    case_id: str
    category: Literal["negative", "load"]
    phase: Literal["deployment", "offline", "vision", "online", "final"]
    title: str
    expected: str
    runner: str
    timeout_seconds: int
    safety: Literal["read_only", "isolated_mutation", "canonical_runtime"]
```

加载器拒绝未知字段、重复 ID、未知前缀、非正数超时、目录软链接和非 UTF-8。目录内容逐条使用
`docs/superpowers/specs/2026-08-12-里程碑2B三卡部署验证设计.md` 第 14 节已经确认的标题与预期，
阶段归属按新设计规格第 5 节固定。

- [ ] **Step 4: 补齐反向测试并运行 GREEN**

覆盖重复 ID、少一条、多一条、未知字段、错误分类、错误阶段、超时为 0、软链接和非法 UTF-8。

Run: `cd algorithm-scheduling-platform && .venv/bin/python -m pytest -q tests/test_milestone_2b_case_catalog.py`

Expected: PASS。

- [ ] **Step 5: 在 OpenSpec tasks 增加五阶段映射**

在第 9 节前增加“2B 真实泳道分期”，只添加新的未完成检查项，不提前勾选：目录与 runner、
FaceRec 新 release、PPT/ASR、视觉、在线、243 总验收。保留现有 4.7-4.12、5.1-5.7、
6.1-6.5、9.1-9.6 和 10.x 的原编号。

- [ ] **Step 6: 提交并推送**

```bash
git add algorithm-scheduling-platform/deploy/milestone-2b-case-catalog.yaml \
  algorithm-scheduling-platform/scripts/milestone_2b_case_catalog.py \
  algorithm-scheduling-platform/tests/test_milestone_2b_case_catalog.py \
  openspec/changes/close-platform-runtime-and-harness-gaps/tasks.md
git commit -m "test(验收): 固化2B反例与压力用例目录"
git push origin codex/milestone-2b-three-gpu-deployment
```

### Task 2: 支持真实执行证据且兼容历史声明

**Files:**
- Modify: `algorithm-scheduling-platform/scripts/milestone_2b_report_contract.py`
- Modify: `algorithm-scheduling-platform/scripts/aggregate_milestone_2b_cases.py`
- Modify: `algorithm-scheduling-platform/scripts/render_milestone_2b_report.py`
- Modify: `algorithm-scheduling-platform/tests/test_milestone_2b_report_aggregation.py`
- Modify: `algorithm-scheduling-platform/tests/test_milestone_2b_report_archive.py`

- [ ] **Step 1: 写新证据 RED 测试**

新 release 的单条执行证据格式为：

```json
{
  "schema_version": 2,
  "evidence_type": "negative_case",
  "case_id": "DEP-001",
  "status": "通过",
  "started_at": "2026-08-18T10:00:00+08:00",
  "finished_at": "2026-08-18T10:00:01+08:00",
  "target": "controlled-target-server",
  "command": "run_milestone_2b_case_batch.py --case DEP-001",
  "reason": "受控错误架构镜像被预检拒绝",
  "mock": false,
  "release_tag": "v1.0_260812",
  "git_sha": "1111111111111111111111111111111111111111",
  "evidence": ["negative/evidence/DEP-001/preflight.json"]
}
```

测试断言：真实证据替换同 ID 的声明；缺失证据文件、跨目录、SHA 不一致、通过但无证据、
重复执行记录均拒绝；旧 schema 1 的历史报告仍可渲染。

- [ ] **Step 2: 运行 RED**

Run: `cd algorithm-scheduling-platform && .venv/bin/python -m pytest -q tests/test_milestone_2b_report_aggregation.py tests/test_milestone_2b_report_archive.py`

Expected: 新 schema 2 用例 FAIL。

- [ ] **Step 3: 增加兼容证据类型**

新增 `negative_execution`、`load_execution` case kind；schema 1 继续只接受
`execution_declaration`，schema 2 接受真实执行文件。聚合规则为“真实执行优先，缺失时生成未执行
声明”，但最终关闭模式增加 `--require-all-executed`，遇到任一声明立即失败。

- [ ] **Step 4: 运行 GREEN 和历史回归**

Run: `cd algorithm-scheduling-platform && .venv/bin/python -m pytest -q tests/test_milestone_2b_report_aggregation.py tests/test_milestone_2b_report_archive.py`

Expected: PASS，旧 release fixture 与新 schema 2 fixture 都通过。

- [ ] **Step 5: 提交并推送**

```bash
git add algorithm-scheduling-platform/scripts/milestone_2b_report_contract.py \
  algorithm-scheduling-platform/scripts/aggregate_milestone_2b_cases.py \
  algorithm-scheduling-platform/scripts/render_milestone_2b_report.py \
  algorithm-scheduling-platform/tests/test_milestone_2b_report_aggregation.py \
  algorithm-scheduling-platform/tests/test_milestone_2b_report_archive.py
git commit -m "feat(验收): 支持2B真实反例与压力证据"
git push origin codex/milestone-2b-three-gpu-deployment
```

### Task 3: 建立安全、有界、可续跑的 case runner

**Files:**
- Create: `algorithm-scheduling-platform/scripts/milestone_2b_case_runners/__init__.py`
- Create: `algorithm-scheduling-platform/scripts/milestone_2b_case_runners/base.py`
- Create: `algorithm-scheduling-platform/scripts/run_milestone_2b_case_batch.py`
- Create: `algorithm-scheduling-platform/tests/test_milestone_2b_case_runner.py`
- Modify: `algorithm-scheduling-platform/pyproject.toml`

- [ ] **Step 1: 写 runner 生命周期 RED 测试**

```python
from collections.abc import Sequence
from dataclasses import dataclass
from pathlib import Path
from typing import Literal, Protocol

@dataclass(frozen=True, slots=True)
class CaseContext:
    release_root: Path
    run_id: str
    target: str
```

```python
class CaseRunner(Protocol):
    async def run(self, context: CaseContext, case: CaseDefinition) -> CaseOutcome:
        raise NotImplementedError

@dataclass(frozen=True, slots=True)
class CaseOutcome:
    status: Literal["通过", "失败"]
    reason: str
    evidence: Sequence[Path]
```

测试覆盖超时、SIGTERM/SIGKILL 回收进程组、异常转失败、write-once、同 ID 续跑拒绝、不同 ID
并行、有界并发、证据必须位于当前 case 目录以及清理失败写入结果。

- [ ] **Step 2: 运行 RED**

Run: `cd algorithm-scheduling-platform && .venv/bin/python -m pytest -q tests/test_milestone_2b_case_runner.py`

Expected: FAIL，runner 尚不存在。

- [ ] **Step 3: 实现执行器**

CLI 支持：

```text
--catalog PATH --release-root PATH --phase NAME
--case CASE_ID --concurrency N --require-cleanup --require-all-selected
```

安全规则：`read_only` 不允许 Docker/数据库变更；`isolated_mutation` 只能操作带当前 run ID 的临时
容器、topic、group、Redis prefix 和 `_test` 数据库；`canonical_runtime` 需要显式持有现有
release-tag 维护锁。所有子进程使用独立进程组和有界超时。

- [ ] **Step 4: 运行 GREEN、Ruff 和 Mypy**

```bash
cd algorithm-scheduling-platform
.venv/bin/python -m pytest -q tests/test_milestone_2b_case_runner.py
.venv/bin/ruff check scripts/milestone_2b_case_catalog.py scripts/milestone_2b_case_runners scripts/run_milestone_2b_case_batch.py tests/test_milestone_2b_case_runner.py
.venv/bin/mypy scripts/milestone_2b_case_catalog.py scripts/milestone_2b_case_runners scripts/run_milestone_2b_case_batch.py
```

Expected: 全部 PASS。

- [ ] **Step 5: 提交并推送**

Commit: `feat(验收): 增加安全可续跑的2B用例执行器`

### Task 4: 实装部署、GPU、注册和基础设施 runner

**Files:**
- Create: `algorithm-scheduling-platform/scripts/milestone_2b_case_runners/deployment.py`
- Create: `algorithm-scheduling-platform/scripts/milestone_2b_case_runners/gpu.py`
- Create: `algorithm-scheduling-platform/scripts/milestone_2b_case_runners/registry.py`
- Create: `algorithm-scheduling-platform/scripts/milestone_2b_case_runners/infrastructure.py`
- Create: `algorithm-scheduling-platform/tests/test_milestone_2b_foundation_case_runners.py`
- Modify: `algorithm-scheduling-platform/deploy/milestone-2b-case-catalog.yaml`

- [ ] **Step 1: 写分组 RED 测试**

测试逐一枚举 `DEP-001..020`、`GPU-001..020`、`REG-001..020`、`INF-001..016`，并断言每个 ID
都映射到具体 runner 方法。禁止使用一个“总是通过”的默认方法。

- [ ] **Step 2: 用受控边界实现 76 条 runner**

执行归属固定如下：

| 分组 | 执行边界 |
| --- | --- |
| DEP | 现有 preflight/build-context/Compose 的临时输入变异，不改 canonical 容器 |
| GPU | fake runtime 单元证据用于格式错误；真实 UUID/PID/进程/停止恢复使用 canonical GPU 容器 |
| REG | 当前 run 独立 Redis prefix 和 control API；DRAINING/TTL/容量均在临时实例 ID 上执行 |
| INF | 当前 run 独立 `_test` 数据库、topic/group、Redis prefix；Mongo 错误使用测试库/错误凭据 |

每个方法必须断言预期中文原因或状态，不得只断言命令返回非零。

- [ ] **Step 3: 运行聚焦和完整部署合同**

```bash
cd algorithm-scheduling-platform
.venv/bin/python -m pytest -q tests/test_milestone_2b_foundation_case_runners.py
.venv/bin/python -m pytest -q tests/test_milestone_2b_scripts.py tests/test_milestone_2b_gpu_evidence.py tests/integration
```

Expected: PASS，无 skipped。

- [ ] **Step 4: 提交并推送**

Commit: `test(部署): 实装2B基础设施反例执行器`

### Task 5: 固化 FaceRec `python3` 回归并完成本地门禁

**Files:**
- Modify: `algorithm-scheduling-platform/tests/test_milestone_2b_gpu_evidence.py`
- Modify: `algorithm-scheduling-platform/tests/test_milestone_2b_scripts.py`
- Modify: `algorithm-scheduling-platform/harness/change-ledger.md`

- [ ] **Step 1: 增加真实 argv 捕获回归**

除现有 `_framework_probe_argv` 单元断言外，fake Docker 必须记录完整 `docker exec` argv，并断言
FaceRec 实际执行 `python3 -c`，其他算子保持现有解释器合同。

- [ ] **Step 2: 运行 GPU 与报告完整回归**

```bash
cd algorithm-scheduling-platform
.venv/bin/python -m pytest -q tests/test_milestone_2b_gpu_evidence.py tests/test_milestone_2b_scripts.py tests/test_milestone_2b_report_aggregation.py
```

Expected: PASS。

- [ ] **Step 3: 更新 Harness 但不宣称远端通过**

台账记录“本地解释器合同通过，远端三卡待新 release”。

- [ ] **Step 4: 提交并推送**

Commit: `test(部署): 锁定FaceRec GPU探针解释器合同`

### Task 6: 在新 release 重跑 FaceRec 与部署层用例

**Files:**
- Modify after execution: `algorithm-scheduling-platform/harness/change-ledger.md`
- Modify after execution: `algorithm-scheduling-platform/harness/architecture-review.md`
- Evidence outside Git: `algorithm-scheduling-platform/deploy/reports/milestone-2b/releases/v1.0_260812/<new-sha>/`

- [ ] **Step 1: 固定新 SHA 并预检远端**

```bash
ssh root@192.168.29.11 'cd /root/workspace/algorithm-scheduling && git fetch origin && git checkout codex/milestone-2b-three-gpu-deployment && git pull --ff-only'
```

确认工作树干净、三卡可见、根分区大于 100 GiB、模型 manifest 通过、基础设施 healthy。

- [ ] **Step 2: 使用前一 release provenance 创建新 release**

显式设置 `PREVIOUS_RELEASE_ROOT` 为旧 SHA 目录；执行 canonical scenario 的 preflight、账本解析、
八镜像构建、revision attestation 和 24 实例启动。不得覆盖旧报告。

- [ ] **Step 3: 重跑 18 个 GPU 实例，重点核对 FaceRec 三实例**

通过条件：三条 FaceRec `framework_gpu_available=true`，真实请求期间有对应物理卡 CUDA PID，
完整容器映射成立，停止后 PID 消失，重启后重新 ONLINE。

- [ ] **Step 4: 执行 deployment phase case batch**

Run: `.venv/bin/python scripts/run_milestone_2b_case_batch.py --catalog deploy/milestone-2b-case-catalog.yaml --release-root "$RELEASE_ROOT" --phase deployment --require-cleanup --require-all-selected`

Expected: 76 条基础反例和分配给 deployment 的 LOAD 用例均产生真实记录；失败照实保留。

- [ ] **Step 5: 清理新增算子并恢复原业务**

只停止 new ledger 中容器，恢复快照，确认无 CUDA PID 残留，不删除平台/基础设施和
`/data/result`。

- [ ] **Step 6: 台账提交并推送**

通过时将 FaceRec/DEC-022 相应边界更新为真实结论；失败时记录实际 case、日志和阻塞，不改写为通过。

Commit: `test(部署): 记录FaceRec三卡复验与基础反例结果`

## 3. 阶段二：PPT 与 ASR 离线泳道

### Task 7: 实现 submission 级媒体下载协调

**Files:**
- Create: `orchestrator_service/app/infrastructure/media.py`
- Modify: `orchestrator_service/app/domain/models.py`
- Modify: `orchestrator_service/app/application/pipeline.py`
- Modify: `orchestrator_service/app/infrastructure/runtime.py`
- Create: `orchestrator_service/tests/test_submission_media.py`
- Modify: `algorithm-scheduling-platform/tests/test_media_download.py`

- [ ] **Step 1: 写 ASR+教师同提交只下载一次的 RED 测试**

```python
paths = await asyncio.gather(
    coordinator.acquire("course-1", "submission-1", "teacher", url),
    coordinator.acquire("course-1", "submission-1", "teacher", url),
)
assert paths[0] == paths[1]
assert downloader.calls == 1
```

另测 `submission-2` 重新下载、`.part` 清理、超时、大小上限、HTML/空视频拒绝和 task ID 路径保护。

- [ ] **Step 2: 运行 RED**

Run: `cd orchestrator_service && ../algorithm-scheduling-platform/.venv/bin/python -m pytest -q tests/test_submission_media.py`

- [ ] **Step 3: 实现 `SubmissionMediaCoordinator`**

使用 `(task_id, submission_id, media_role)` 锁和缓存；最终路径位于
`/data/course/{task_id}/submissions/{submission_id}/{teacher|student|slides}.mp4`，下载完成前只写
`.part`，响应体、Content-Type、大小和 ffprobe 均通过后原子替换。

- [ ] **Step 4: 运行 GREEN 和回归**

Run: `cd orchestrator_service && ../algorithm-scheduling-platform/.venv/bin/python -m pytest -q tests/test_submission_media.py && cd ../algorithm-scheduling-platform && .venv/bin/python -m pytest -q tests/test_media_download.py`

- [ ] **Step 5: 提交并推送**

Commit: `feat(编排): 增加提交级媒体下载协调`

### Task 8: 装配异步 PPT 切片、续租、回调与对账

**Files:**
- Create: `orchestrator_service/app/infrastructure/ppt_runtime.py`
- Create: `orchestrator_service/app/application/node_adapters.py`
- Modify: `orchestrator_service/app/application/executor.py`
- Modify: `orchestrator_service/app/infrastructure/runtime.py`
- Modify: `orchestrator_service/app/api/routes.py`
- Modify: `orchestrator_service/app/core/config.py`
- Modify: `orchestrator_service/config.toml`
- Create: `orchestrator_service/tests/test_ppt_runtime.py`
- Modify: `algorithm-scheduling-platform/tests/test_ppt_slice_adapter.py`

- [ ] **Step 1: 写 PPT 生命周期 RED 测试**

覆盖：状态 50 才进入异步等待、accepted 身份一致、租约周期续期、终态回调幂等、manifest 对账
恢复丢失回调、损坏/越界/count 不一致拒绝、终态事务后释放租约。

- [ ] **Step 2: 运行 RED**

Run: `cd orchestrator_service && ../algorithm-scheduling-platform/.venv/bin/python -m pytest -q tests/test_ppt_runtime.py`

- [ ] **Step 3: 实现 `PptRuntimeCoordinator`**

同步 `NodeExecutor` 遇到 `PPT_SLICE` 时只提交并登记异步运行项；后台协调器按配置续租和对账。
回调路由只接收既有 `PptSliceTerminalCallback`，调用 `PptSliceCallbackHandler` 校验并完成节点。

- [ ] **Step 4: 运行 GREEN 和 PPT 合同回归**

```bash
cd orchestrator_service
../algorithm-scheduling-platform/.venv/bin/python -m pytest -q tests/test_ppt_runtime.py
cd ../algorithm-scheduling-platform
.venv/bin/python -m pytest -q tests/test_ppt_slice_adapter.py tests/test_ppt_work_planner.py
```

- [ ] **Step 5: 提交并推送**

Commit: `feat(编排): 接通PPT异步终态与容量续约`

### Task 9: 装配 PPT OCR 和关键词子项执行

**Files:**
- Modify: `orchestrator_service/app/application/node_adapters.py`
- Modify: `orchestrator_service/app/infrastructure/ppt_text.py`
- Modify: `orchestrator_service/app/infrastructure/runtime.py`
- Modify: `orchestrator_service/app/core/config.py`
- Create: `orchestrator_service/tests/test_ppt_text_runtime.py`
- Modify: `algorithm-scheduling-platform/tests/test_ppt_text_pipeline.py`

- [ ] **Step 1: 写按 `ppt_image_id` 部分进度 RED 测试**

断言 OCR/关键词各自申请对应能力租约；每图完成即写 work item；空 OCR 文本不调用 LLM；部分失败
保留成功项和中文失败原因；重复执行不产生重复 item。

- [ ] **Step 2: 运行 RED，装配最小实现，再运行 GREEN**

Run: `cd orchestrator_service && ../algorithm-scheduling-platform/.venv/bin/python -m pytest -q tests/test_ppt_text_runtime.py`

实现时复用 `PptTextPipeline`，不把 Base64 或 OCR/关键词 JSON 写入 Kafka。

- [ ] **Step 3: 提交并推送**

Commit: `feat(编排): 接通PPT文字与关键词子任务`

### Task 10: 装配离线 ASR 与课程脑图

**Files:**
- Modify: `orchestrator_service/app/application/node_adapters.py`
- Modify: `orchestrator_service/app/infrastructure/asr.py`
- Modify: `orchestrator_service/app/infrastructure/audio.py`
- Modify: `orchestrator_service/app/infrastructure/course_overview.py`
- Modify: `orchestrator_service/app/infrastructure/runtime.py`
- Create: `orchestrator_service/tests/test_asr_runtime.py`
- Modify: `algorithm-scheduling-platform/tests/test_offline_asr_adapter.py`
- Modify: `algorithm-scheduling-platform/tests/test_course_overview_adapter.py`

- [ ] **Step 1: 写完整 v1.1.8 与 effective_params RED 测试**

默认参数固定为 `showSpk=true`、`showEmotion=true`、`showRoleIdentify=false`；A 提供值覆盖默认值，
`wordTimestamps` 不固化但传入时透传并记录。完整 ASR 响应持久化，脑图只读取保存的 segments。

- [ ] **Step 2: 写 WAV、业务码、租约和依赖测试**

覆盖 16kHz/单声道/16-bit、空音频、无音轨、HTTP 超时、非成功业务码、空 segments、长任务续租、
脑图前置依赖、GenericResponse 嵌套校验和 LLM 故障不破坏 ASR 结果。

- [ ] **Step 3: 运行 RED，装配真实 adapter，再运行 GREEN**

```bash
cd orchestrator_service
../algorithm-scheduling-platform/.venv/bin/python -m pytest -q tests/test_asr_runtime.py
cd ../algorithm-scheduling-platform
.venv/bin/python -m pytest -q tests/test_audio_extraction.py tests/test_offline_asr_adapter.py tests/test_course_overview_adapter.py
```

- [ ] **Step 4: 提交并推送**

Commit: `feat(编排): 接通离线转写与课程脑图`

### Task 11: 建立 PPT/ASR 真实 E2E 和所属 case runner

**Files:**
- Create: `algorithm-scheduling-platform/tests/integration/test_offline_lanes_runtime.py`
- Create: `algorithm-scheduling-platform/scripts/milestone_2b_case_runners/offline.py`
- Create: `algorithm-scheduling-platform/tests/test_milestone_2b_offline_case_runners.py`
- Create: `algorithm-scheduling-platform/scripts/run_offline_lanes.py`
- Modify: `algorithm-scheduling-platform/deploy/milestone-2b-case-catalog.yaml`
- Modify: `algorithm-scheduling-platform/harness/scenarios/milestone-2b-business-lanes-closure.md`

- [ ] **Step 1: 写真实服务 E2E RED 测试**

使用真实 PostgreSQL/Redis/Kafka、control/orchestrator 进程和 HTTP 算子替身，覆盖 PPT-only、ASR-only、
PPT+ASR、结果复用、task type 追加、同 submission 下载共享和后续 submission 重新下载。状态只能由
运行中的 Worker 推进。

- [ ] **Step 2: 实现一键 runner 并运行 GREEN**

Run: `cd algorithm-scheduling-platform && .venv/bin/python scripts/run_offline_lanes.py`

Expected: 无 skipped；报告包含请求、Outbox、Kafka offset、节点、租约、文件和查询结果。

- [ ] **Step 3: 实装 JOB、FILE、PPT、OCR、KEY、ASR runner**

逐一覆盖 `JOB-001..020`、`FILE-001..016`、`PPT-001..015`、`OCR-001..005`、
`KEY-001..005`、`ASR-001..018`，B 级人工判断项保存允许留存的样本路径和结论。

- [ ] **Step 4: 提交并推送**

Commit: `test(泳道): 增加PPT与ASR完整链路验收`

## 4. 阶段三：视觉泳道

### Task 12: 装配视觉 Kafka runtime 与课程恢复

**Files:**
- Create: `vision_orchestrator_service/app/application/runtime.py`
- Modify: `vision_orchestrator_service/app/application/factory.py`
- Modify: `vision_orchestrator_service/app/application/events.py`
- Modify: `vision_orchestrator_service/app/main.py`
- Modify: `vision_orchestrator_service/app/core/config.py`
- Modify: `vision_orchestrator_service/config.toml`
- Modify: `orchestrator_service/app/application/vision_events.py`
- Modify: `orchestrator_service/app/infrastructure/runtime.py`
- Create: `vision_orchestrator_service/tests/test_runtime.py`
- Create: `algorithm-scheduling-platform/tests/integration/test_visual_kafka_runtime.py`

- [ ] **Step 1: 写 command/event、幂等和重启 RED 测试**

课程命令处理成功写入本地持久化事实后提交 offset；进度/完成事件幂等；晚到进度不能回退终态；
vision 重启恢复未完成课程且不重复最终结果。

- [ ] **Step 2: 实现 lifespan runtime**

启动真实 consumer/producer、HTTP client 和任务恢复循环；Kafka handler 只接收课程级命令，立即转入
PostgreSQL 任务事实，不在 poll 边界执行整节课。

- [ ] **Step 3: 运行 GREEN 和关闭回归**

Run: `cd algorithm-scheduling-platform && .venv/bin/python -m pytest -q tests/integration/test_visual_kafka_runtime.py && cd ../vision_orchestrator_service && ../algorithm-scheduling-platform/.venv/bin/python -m pytest -q tests/test_runtime.py`

- [ ] **Step 4: 提交并推送**

Commit: `feat(视觉): 接通课程级Kafka运行时`

### Task 13: 组合抽帧、自适应 VBas、聚合和证据

**Files:**
- Create: `vision_orchestrator_service/app/infrastructure/frame_extractor.py`
- Create: `vision_orchestrator_service/app/application/analyzer.py`
- Modify: `vision_orchestrator_service/app/application/runtime.py`
- Modify: `vision_orchestrator_service/app/domain/adaptive_scan.py`
- Modify: `vision_orchestrator_service/app/domain/behavior_intervals.py`
- Modify: `vision_orchestrator_service/app/domain/student_aggregation.py`
- Modify: `vision_orchestrator_service/app/domain/evidence.py`
- Create: `vision_orchestrator_service/tests/test_analyzer.py`
- Create: `algorithm-scheduling-platform/scripts/milestone_2b_case_runners/vision.py`
- Create: `algorithm-scheduling-platform/tests/test_milestone_2b_vision_case_runners.py`

- [ ] **Step 1: 写粗扫/10/5/2/1 秒细化 RED 测试**

确定性帧结果模拟 19:50 有板书、20:10 无板书，断言继续 5/2/1 秒定位边界；板书 gap<=3 秒合并、
>3 秒分段，坐姿 gap<=5 秒合并，单帧异常不立即切断。

- [ ] **Step 2: 写学生聚合和证据 RED 测试**

覆盖有/无 front/back 区域、单边提供、稳定兜底、识别人数大于 student_count、空行为、有效帧不足、
证据数量上限、路径保护和结果目录保留。

- [ ] **Step 3: 实现 `VisualAnalyzer` 并运行 GREEN**

抽帧写临时目录；相同时间戳通过 cache 去重；每批 VBas 请求通过 control 租约路由；精选证据原子写入
`/data/result/{task_id}/vision`；结构化结果通过完成事件返回 orchestrator。

- [ ] **Step 4: 实装 `VIS-001..028` runner 并回归**

Run: `cd algorithm-scheduling-platform && .venv/bin/python -m pytest -q tests/test_milestone_2b_vision_case_runners.py tests/test_adaptive_vision_scan.py tests/test_behavior_intervals.py tests/test_student_aggregation.py tests/test_vision_evidence.py tests/test_vbas_batch_client.py`

- [ ] **Step 5: 提交并推送**

Commit: `feat(视觉): 接通自适应检测聚合与证据`

## 5. 阶段四：在线泳道

### Task 14: 完成人脸管理代理与在线资源关闭

**Files:**
- Create: `online_gateway_service/app/infrastructure/persons.py`
- Modify: `online_gateway_service/app/api/routes.py`
- Modify: `online_gateway_service/app/application/factory.py`
- Modify: `online_gateway_service/app/core/config.py`
- Modify: `online_gateway_service/config.toml`
- Create: `online_gateway_service/tests/test_person_routes.py`
- Modify: `online_gateway_service/tests/test_service_project.py`

- [ ] **Step 1: 写人物创建、更新、查询、删除 RED 测试**

所有请求先申请 `facerec` 租约，再代理现有 `/persons` 合同；MongoDB 不由网关直连。测试覆盖 Mongo
不可用、无人脸、重复人物、删除不存在人物、三实例共享库和 `save_person_photo=false`。

- [ ] **Step 2: 实现 `FacePersonClient` 和路由**

保持 FaceRec 请求/响应字段，不在网关重新定义 embedding；HTTP client 在 lifespan 关闭。

- [ ] **Step 3: 运行 GREEN**

Run: `cd online_gateway_service && ../algorithm-scheduling-platform/.venv/bin/python -m pytest -q tests/test_person_routes.py tests/test_service_project.py`

- [ ] **Step 4: 提交并推送**

Commit: `feat(在线): 增加FaceRec人物管理代理`

### Task 15: 完整验证在线图片和实时 ASR

**Files:**
- Modify: `online_gateway_service/app/api/routes.py`
- Modify: `online_gateway_service/app/infrastructure/websocket_proxy.py`
- Modify: `online_gateway_service/app/infrastructure/capacity.py`
- Modify: `online_gateway_service/tests/test_service_project.py`
- Create: `algorithm-scheduling-platform/tests/integration/test_online_lanes_runtime.py`
- Create: `algorithm-scheduling-platform/scripts/milestone_2b_case_runners/online.py`
- Create: `algorithm-scheduling-platform/tests/test_milestone_2b_online_case_runners.py`

- [ ] **Step 1: 写三个图片入口 RED/回归测试**

固定路径 `/api/online/vbas/analyze`、`/api/online/face/recognize`、
`/api/online/image-quality/detect`。整请求只选一个实例，多图不拆分；验证 Base64、大小上限、容量不足、
非 JSON 算子响应、ScreenDet 模块结果透传、无 Kafka 和无媒体下载。

- [ ] **Step 2: 写实时 ASR 粘性和断开测试**

同一 WebSocket 只使用一个 `asr_online` 实例；无容量以 1013 关闭；上游连接失败、实例中途下线、
客户端异常断开都必须有界关闭并释放租约；结果不得写入离线课程库。

- [ ] **Step 3: 实装 `ONL-001..020`、`FACE-001..014` runner**

每条 runner 通过 `online-gateway-service`，不得绕过网关直接调用算子作为通过证据。

- [ ] **Step 4: 运行 GREEN 和完整在线回归**

```bash
cd algorithm-scheduling-platform
.venv/bin/python -m pytest -q tests/integration/test_online_lanes_runtime.py tests/test_milestone_2b_online_case_runners.py tests/test_online_gateway.py
cd ../online_gateway_service
../algorithm-scheduling-platform/.venv/bin/python -m pytest -q
```

- [ ] **Step 5: 提交并推送**

Commit: `test(在线): 完成图片与实时转写泳道验收`

## 6. 阶段五：压力、可观测性和完整验收

### Task 16: 接通清理、审计和运维快照

**Files:**
- Modify: `orchestrator_service/app/application/executor.py`
- Modify: `orchestrator_service/app/infrastructure/runtime.py`
- Modify: `control_service/app/api/routes.py`
- Modify: `algorithm-scheduling-platform/packages/platform_common/metrics.py`
- Modify: `algorithm-scheduling-platform/packages/platform_common/logging.py`
- Modify: `algorithm-scheduling-platform/packages/platform_common/workspace.py`
- Modify: `algorithm-scheduling-platform/tests/test_platform_metrics.py`
- Modify: `algorithm-scheduling-platform/tests/test_workspace_cleanup.py`
- Modify: `algorithm-scheduling-platform/tests/test_operations_api.py`

- [ ] **Step 1: 写终态清理和审计 RED 测试**

终态清理只删除当前任务临时目录，绝不删除 `/data/result`；清理失败不回退业务终态。审计记录 task、
node、attempt、trace、instance、model、duration 和 result status。

- [ ] **Step 2: 写运维快照 RED 测试**

快照包含节点状态、Kafka lag、Outbox、租约、实例 GPU 标签、磁盘和真实 node code；后台循环失败时
readiness 和快照均显示中文原因。

- [ ] **Step 3: 实现并运行 GREEN**

Run: `cd algorithm-scheduling-platform && .venv/bin/python -m pytest -q tests/test_platform_metrics.py tests/test_workspace_cleanup.py tests/test_operations_api.py`

- [ ] **Step 4: 提交并推送**

Commit: `feat(运维): 接通任务清理审计与运行快照`

### Task 17: 实装 26 条压力与恢复 runner

**Files:**
- Create: `algorithm-scheduling-platform/scripts/milestone_2b_case_runners/load.py`
- Create: `algorithm-scheduling-platform/tests/test_milestone_2b_load_case_runners.py`
- Create: `algorithm-scheduling-platform/scripts/run_milestone_2b_load.py`
- Modify: `algorithm-scheduling-platform/deploy/milestone-2b-case-catalog.yaml`
- Modify: `algorithm-scheduling-platform/harness/scenarios/milestone-2b-business-lanes-closure.md`

- [ ] **Step 1: 写逐级加压和停止条件 RED 测试**

阶段固定为单请求、3、10、30、100；任一 OOM、Xid、持续错误、磁盘低于 100 GiB、lag 不回落或
readiness 失败立即停止升级。离线课程和在线单图分开统计。

- [ ] **Step 2: 实现资源采样和结果 schema**

每级记录请求数、并发、成功/失败、业务码、P50/P95/P99、实例选择、inflight、租约、Kafka lag、
Outbox、GPU 显存/利用率、CPU、内存、磁盘和恢复时间。

- [ ] **Step 3: 实装 `LOAD-001..026`**

恢复类使用独立测试资源或 canonical 维护锁；SIGKILL、Kafka/Redis/PostgreSQL 重启均验证恢复事实；
证据真实性和清理用例通过故意构造的受控坏报告验证 Harness fail-closed。

- [ ] **Step 4: 运行聚焦测试**

Run: `cd algorithm-scheduling-platform && .venv/bin/python -m pytest -q tests/test_milestone_2b_load_case_runners.py`

- [ ] **Step 5: 提交并推送**

Commit: `test(压测): 增加2B渐进压力与恢复执行器`

### Task 18: 本地完整回归和 OpenSpec 状态更新

**Files:**
- Modify: `openspec/changes/close-platform-runtime-and-harness-gaps/tasks.md`
- Modify: `algorithm-scheduling-platform/harness/architecture-review.md`
- Modify: `algorithm-scheduling-platform/harness/change-ledger.md`
- Modify: `control_service/README.md`
- Modify: `control_service/config.toml`
- Modify: `orchestrator_service/README.md`
- Modify: `orchestrator_service/config.toml`
- Modify: `vision_orchestrator_service/README.md`
- Modify: `vision_orchestrator_service/config.toml`
- Modify: `online_gateway_service/README.md`
- Modify: `online_gateway_service/config.toml`

- [ ] **Step 1: 运行静态、类型和服务测试**

```bash
cd algorithm-scheduling-platform
.venv/bin/ruff check . ../control_service ../orchestrator_service ../vision_orchestrator_service ../online_gateway_service
.venv/bin/mypy packages ../control_service/app ../orchestrator_service/app ../vision_orchestrator_service/app ../online_gateway_service/app
.venv/bin/python -m pytest -q
make service-test
docker compose -f deploy/docker-compose.infrastructure.yml config --quiet
docker compose -f deploy/docker-compose.platform.yml config --quiet
docker compose -f deploy/docker-compose.operators.yml config --quiet
cd ..
openspec validate close-platform-runtime-and-harness-gaps --strict
```

Expected: 全部 PASS，无 skipped。任何失败记录 Harness 并修复后重跑。

- [ ] **Step 2: 只勾选已有证据任务**

对照运行证据更新 4.7-4.12、5.1-5.7、6.1-6.5、9.1-9.6；未产生真实证据的项目保持未完成。

- [ ] **Step 3: 提交并推送**

Commit: `chore(调度): 完成真实业务泳道本地验收`

### Task 19: 远端运行四类真实泳道和全部 243 条用例

**Files:**
- Evidence outside Git: `algorithm-scheduling-platform/deploy/reports/milestone-2b/releases/<tag>/<sha>/`
- Modify after execution: `algorithm-scheduling-platform/harness/change-ledger.md`
- Modify after execution: `algorithm-scheduling-platform/harness/architecture-review.md`
- Modify after execution: `openspec/changes/close-platform-runtime-and-harness-gaps/tasks.md`

- [ ] **Step 1: 新 SHA 全栈部署**

按 canonical 2B 场景完成预检、镜像、24 实例、18 GPU、6 CPU、注册、Smoke 和恢复；确认所有镜像
revision 等于当前 SHA。

- [ ] **Step 2: 运行 PPT/ASR 泳道**

使用 `http://192.168.29.12:5555/course/` 的真实 T/S/P 视频，保存 A 请求、状态变化、结果、文件、
模型输出和人工 B 级判断。

- [ ] **Step 3: 运行视觉泳道**

执行 teacher-only、student-only 和组合任务，保存粗扫/细化轮次、VBas 实例、区间、人数、兜底标识
和证据图片。

- [ ] **Step 4: 运行在线泳道**

执行三个图片入口、人物管理和实时 ASR WebSocket，验证请求级路由、会话粘性、容量和释放。

- [ ] **Step 5: 重新运行全部 243 条**

```bash
.venv/bin/python scripts/run_milestone_2b_case_batch.py \
  --catalog deploy/milestone-2b-case-catalog.yaml \
  --release-root "$RELEASE_ROOT" --phase final \
  --require-cleanup --require-all-selected
.venv/bin/python scripts/aggregate_milestone_2b_cases.py \
  --release-root "$RELEASE_ROOT" \
  --operator-compose deploy/docker-compose.operators.yml \
  --smoke-manifest deploy/operator-smoke-cases.json \
  --report-plan deploy/milestone-2b-report-plan.json \
  --require-all-executed \
  --output "$RELEASE_ROOT/summary/cases.json"
```

Expected: 217 negative + 26 load 全部有真实执行记录，没有“未执行及原因”。

- [ ] **Step 6: 渲染、清理和结论**

任何失败均保留 `overall_status=失败` 并提交 Harness 事实；全部通过时才把 DEC-024 和对应 OpenSpec
任务标记完成。停止新增算子、恢复原业务、确认无临时 topic/group/key/database/process/CUDA PID，
保留 `/data/result`。

- [ ] **Step 7: 最终提交并推送**

通过：`test(验收): 完成2B真实泳道与243条用例`

失败：`test(验收): 记录2B完整验收失败事实`

### Task 20: 最终复审与变更关闭判断

**Files:**
- Modify: `docs/算法功能调度平台总体设计-v2.md`
- Regenerate: `docs/算法功能调度平台总体设计-v2.pdf`
- Modify: `algorithm-scheduling-platform/harness/architecture-review.md`
- Modify: `algorithm-scheduling-platform/harness/change-ledger.md`
- Modify: `openspec/changes/close-platform-runtime-and-harness-gaps/tasks.md`

- [ ] **Step 1: 重跑全部验证命令**

重复 Task 18 的完整门禁，并验证 PDF Mermaid、页码、表格和中文字体渲染。

- [ ] **Step 2: 复审 OpenSpec 完成事实**

只有任务全完成且总报告通过，才执行 OpenSpec sync/archive；有失败或未执行时保持 change active。

- [ ] **Step 3: 提交并推送**

Commit: `docs(架构): 同步2B完整验收结论`

## 7. 计划完成门槛

- FaceRec 三实例在新 release 中通过完整 GPU 证据链。
- PPT、ASR、视觉、在线均由平台入口调用真实算子完成。
- 217 条反例和 26 条压力用例全部实际执行。
- 最终报告失败数为 0、未执行数为 0。
- OpenSpec、完整测试、类型、lint、Compose、镜像和 Harness 门禁全部通过。
- 旧报告不可变，用户已有文件未被纳入提交，所有提交已推送。
