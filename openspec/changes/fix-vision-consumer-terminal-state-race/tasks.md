## 1. 代码修复

- [x] 1.1 在 `VisualCommandProcessor` 中引入 Repository 状态冲突的专门判定和终态二次读取。
- [x] 1.2 为进度更新竞态增加幂等分支，保留非终态和基础设施错误的失败语义。
- [x] 1.3 为完成结果竞态增加幂等分支，确保不覆盖终态结果。

## 2. 回归测试

- [x] 2.1 增加 `COMPLETED`、`FAILED`、`CANCELLED` 的迟到进度测试。
- [x] 2.2 增加完成阶段已完成、已失败/取消和非终态冲突测试。
- [x] 2.3 增加节点不存在、数据库错误、Producer 错误不提交 offset 的测试。
- [x] 2.4 增加竞态消息后续消息继续处理与连续 offset 提交测试。

## 3. 验证与证据

- [x] 3.1 运行视觉 Consumer 和 Orchestrator 视觉事件回归测试。
- [x] 3.2 运行 `compileall`、应用导入和相关静态检查。
- [x] 3.3 在 Harness 记录复现、修复前后行为和验证命令。
- [x] 3.4 复审 OpenSpec 需求与实现一致性。
