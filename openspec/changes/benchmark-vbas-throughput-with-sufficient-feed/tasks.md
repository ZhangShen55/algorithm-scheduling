## 1. 基准资产与执行合同

- [ ] 1.1 盘点 VBas 教师、学生离线接口及其现有请求响应合同，确认测试不修改路径、字段和模型输出
- [ ] 1.2 从真实课程视频准备固定教师帧和学生帧语料，覆盖常见画面、空画面和多人画面
- [ ] 1.3 生成 fixture manifest，记录图片相对路径、字节数、分辨率、类别和 SHA-256
- [ ] 1.4 定义 write-once `campaign_id`、attempt、随机种子、Git SHA、镜像 ID、配置摘要和证据目录格式
- [ ] 1.5 定义并发阶梯、预热时长、稳态时长、最小 batch 数、重复次数及可配置测试上界
- [ ] 1.6 定义显存、错误率、延迟恶化、OOM、容器重启和负载器供料不足的自动停止护栏

## 2. VBas 持续供料压测器

- [ ] 2.1 实现预构造请求并按指定实例地址直连 VBas 的异步批次负载器
- [ ] 2.2 实现每实例独立并发窗口和请求完成后立即补位，记录目标在途、实际在途和补位延迟
- [ ] 2.3 实现教师、学生以及固定 50%/50% 顺序的混合负载模式
- [ ] 2.4 实现 `8 -> 6 -> 4 -> 3 -> 2 -> 1` 默认降阶和可配置高位，禁止使用无限或未解析的理论最大值
- [ ] 2.5 实现单实例与三实例同步起测、定时停止、优雅排空和非零错误退出
- [ ] 2.6 统计 batch/s、frame/s、P50/P95/P99、成功、业务错误、超时、连接错误和补位延迟
- [ ] 2.7 检测客户端 CPU、连接池和事件循环是否限制实际在途，供料器受限时失败关闭
- [ ] 2.8 为负载器的确定性序列、并发窗口、停止护栏、统计聚合和中断恢复补充自动化测试

## 3. GPU 与显存稳定性采集

- [ ] 3.1 实现按实例和 GPU 采集显存、GPU 利用率、功耗、进程状态及容器重启次数的时间序列
- [ ] 3.2 分别标记模型就绪 `M_ready`、预热结束 `M_warm`、稳态窗口和测试结束采样点
- [ ] 3.3 实现稳态前后窗口差异、趋势斜率、P50/P95/峰值和硬护栏判断
- [ ] 3.4 将稳定缓存平台与持续膨胀分别报告，不以高于启动基线作为单一失败条件
- [ ] 3.5 验证 OOM、显存硬护栏、容器重启和采集器中断均能终止当前档并保留证据

## 4. 媒体供料隔离基准

- [ ] 4.1 为 Vision 增加仅用于测试的零延迟 batch sink 或等价隔离 Harness，不申请租约且不调用 VBas
- [ ] 4.2 记录视觉命令开始、ffprobe 排队/执行、抽帧排队/执行、batch ready 和 sink 完成时间
- [ ] 4.3 在应用层媒体槽位不小于活动流数量的条件下执行 `16 -> 12 -> 8 -> 6 -> 4 -> 2 -> 1` 活动流降阶
- [ ] 4.4 采集 `prepared batch/s`、首批耗时、批间隔 P95/P99、FFmpeg 进程/线程、CPU、load average、iowait 和内存
- [ ] 4.5 判断降低活动流后吞吐是否上升，并标识 CPU、磁盘、进程争用或视觉命令启动瓶颈
- [ ] 4.6 为零延迟 sink 不触发 VBas、时间戳完整性和媒体阶段失败关闭补充测试

## 5. 租约与分发隔离基准

- [ ] 5.1 实现预抽帧 batch 回放模式，保留 Control `offline` 租约、实例路由和真实 VBas HTTP 调用
- [ ] 5.2 记录 `frame_batch_ready_at`、`lease_requested_at`、`lease_acquired_at`、`vbas_started_at` 和 `vbas_finished_at`
- [ ] 5.3 统计 ready queue 深度、租约快照等待、权威租约拒绝、HTTP 启动延迟和实例分配差异
- [ ] 5.4 实现“ready 不足则归因供料、ready 充足但槽位空闲则归因分发”的自动判定
- [ ] 5.5 验证三实例实例选择均衡、租约严格释放、瞬时竞争重试和无租约不调用算子

## 6. 本地验证与静态门禁

- [ ] 6.1 对新增测试工具运行格式、静态检查和编译门禁
- [ ] 6.2 运行 VBas 请求生成、并发补位、统计和显存趋势判断单元测试
- [ ] 6.3 运行媒体 sink 与预抽帧回放的 Vision 集成测试
- [ ] 6.4 运行现有 VBas 教师和学生接口回归，确认 HTTP 合同与响应结构未改变
- [ ] 6.5 运行平台相关回归，确认测试接入点不会在普通生产配置中启用

## 7. 远端预检与 VBas 极限阶梯

- [ ] 7.1 记录 `192.168.29.11` 三卡型号、总显存、驱动、容器、镜像 revision、VBas 有效配置和初始显存
- [ ] 7.2 验证三个 VBas 实例模型就绪、GPU 归属正确、共享 fixture 可读且日志目录可写
- [ ] 7.3 分别执行教师单实例高位到低位阶梯并保留每档独立证据
- [ ] 7.4 分别执行学生单实例高位到低位阶梯并保留每档独立证据
- [ ] 7.5 执行三实例教师、学生及 50%/50% 混合高位到低位阶梯
- [ ] 7.6 对每档重启三个 VBas 恢复显存基线，并核验新一档没有继承前一档进程或请求
- [ ] 7.7 计算教师、学生、混合的最高稳定吞吐档和 95% 吞吐最低并发拐点
- [ ] 7.8 对候选拐点至少重复两轮，吞吐偏差超过 5% 时保持未收敛并继续调查

## 8. 远端供料与分发定位

- [ ] 8.1 使用固定本地视频执行纯媒体活动流降阶并形成 FFmpeg/CPU/磁盘吞吐曲线
- [ ] 8.2 使用固定预抽帧集合执行真实租约和 VBas 分发回放
- [ ] 8.3 对比 VBas 裸能力 `C_vbas`、媒体供料 `C_feed` 和预抽帧分发 `C_dispatch`
- [ ] 8.4 验证 `C_feed` 是否达到持续供满推荐 VBas 槽位所需速率，并量化缺口
- [ ] 8.5 按 ready queue 与槽位状态统计供料空档、分发空档、最长空档和累计时间
- [ ] 8.6 输出明确的瓶颈结论，禁止将容量等待、抽帧、命令启动和 VBas 服务时间混为同一指标

## 9. 完整链路回验

- [ ] 9.1 使用固定课程和全新任务 ID 执行当前配置 D0 基线，记录全部视觉阶段时间
- [ ] 9.2 将隔离测试候选参数应用到完整链路，每轮只调整一个变量或直接依赖的一组变量
- [ ] 9.3 验证三实例同时空闲比例、平均活跃槽位、六槽并发时间和实例任务量差异
- [ ] 9.4 验证视觉任务成功率 100%、Consumer 健康、租约归零、临时媒体清理和无容器重启
- [ ] 9.5 验证完整链路显存形成稳定平台且不存在随任务数量持续增长
- [ ] 9.6 若完整链路未达到目标，使用隔离证据精确归因并保留失败结果，不直接扩大 VBas 容量重试

## 10. 发布、Harness 与结论

- [ ] 10.1 若实现修改了运行时源码，使用现有 BuildKit 缓存仅构建实际受影响的最新镜像
- [ ] 10.2 新容器通过 revision、架构、health、readiness 和 Consumer 门禁后精确删除被替换的旧容器和旧镜像
- [ ] 10.3 若门禁失败，恢复原镜像和配置并保留失败构建、部署及回滚证据
- [ ] 10.4 在 Harness 中记录每个 attempt 的配置、fixture、原始指标、停止原因、计算过程和中文结论
- [ ] 10.5 输出 VBas 性能极限、推荐并发拐点、媒体供料上限、分发损耗和完整链路效率报告
- [ ] 10.6 更新相关部署或测试文档，明确推荐参数适用的硬件、镜像 revision 和负载类型
- [ ] 10.7 复核所有用例已完整终结，任何中断或缺证据项不得标记完成
