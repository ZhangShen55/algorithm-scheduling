## 1. 参数规范化

- [x] 1.1 将 `control_service` 的 ASR 默认值改为 `showSpk=false`、`showEmotion=false`，并补充默认值单元测试
- [x] 1.2 实现完整 `effective_params` 的规范化序列化和 `params_fingerprint` 计算
- [x] 1.3 校验部分参数、未知字段和非法类型的处理结果，确保现有参数校验语义不被破坏

## 2. 数据库执行版本

- [x] 2.1 设计并执行 PostgreSQL 迁移，增加 ASR 执行版本表、参数指纹、状态、原因、结果引用和时间字段
- [x] 2.2 为执行版本增加唯一性、活动版本和历史查询所需的索引及中文字段注释
- [x] 2.3 编写已有 ASR 任务的迁移逻辑，按原 `effective_params` 补齐默认值并生成初始指纹
- [x] 2.4 扩展 Repository，支持查找成功版本、查找活动版本、创建新版本和读取历史版本

## 3. 控制面提交与查询

- [x] 3.1 修改 ASR 提交流程，按参数指纹选择复用、返回活动版本或创建新执行版本
- [x] 3.2 在事务中创建执行版本并写入携带 `run_id` 的 Outbox 事件，由编排器消费后初始化节点
- [x] 3.3 扩展提交和查询响应，返回选中的 `run_id`、完整 `effective_params`、状态、中文 `reason` 和对应结果
- [x] 3.4 增加历史执行版本摘要查询，未指定参数时返回最近一次请求对应的版本
- [x] 3.5 增加并发提交测试，验证相同参数只创建一个活动版本和一条 Outbox 事件

## 4. 编排器运行时

- [x] 4.1 让 `orchestrator_service` 消费并校验 `run_id`、参数指纹和完整 `effective_params`
- [x] 4.2 让 DAG 节点按执行版本隔离，支持不同 ASR 参数版本顺序或并行执行
- [x] 4.3 结果回写时同步更新执行版本和课程 ASR 汇总状态，保留历史结果
- [x] 4.4 兼容迁移前不含 `run_id` 的旧 Outbox 事件，并增加重复消费测试

## 5. 验证与文档

- [x] 5.1 增加 false/false 默认参数端到端测试
- [x] 5.2 增加 false/false 到 true/true 的重新调度测试
- [x] 5.3 增加 true/true 已完成结果复用和失败版本再次执行测试
- [x] 5.4 使用现有 ASR 算子执行一次真实任务，确认请求字段和响应格式兼容
- [x] 5.5 更新控制面、编排器和 A 服务对接文档，说明参数版本和 `run_id` 语义
