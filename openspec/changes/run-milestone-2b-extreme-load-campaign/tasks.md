## 1. 变更基线与安全边界

- [x] 1.1 记录当前分支、完整 Git SHA、dirty/untracked 文件和用户不纳管文件，实施过程不覆盖、删除或提交这些文件。
- [x] 1.2 核对 `standardize-service-file-logging` 和 `retire-text-analysis-from-scheduling-platform` 的当前必需任务，确定用于 Campaign 的唯一 clean Git SHA 已包含日志合同和七算子基线。
- [x] 1.3 只读盘点 `192.168.29.11` 的宿主机资源、容器、镜像、数据卷、端口、三 GPU、`/data/course`、`/data/result` 和当前 release，不在基线阶段执行删除。
- [x] 1.4 记录负载主机的 CPU、内存、网络、socket、文件句柄和 Python 运行环境，确认负载主机不与目标服务器共享主要计算资源。
- [x] 1.5 建立本变更的 Harness 场景和 change-ledger 初始记录，标注当前证据级层、已知 ASR-013 质量阻断和尚未执行的远端阶段。

## 2. Campaign 用例目录与配置权威

- [x] 2.1 先增加 catalog schema 测试，要求每条用例具有唯一 ID、阶段、前置、负载、fixture、预期、超时、护栏、清理和证据路径。
- [x] 2.2 实现可版本化的 A 服务 Campaign catalog，覆盖 baseline、offline、control-query、online、mixed、recovery 和 soak 七个阶段。
- [x] 2.3 为短 T/S/P 片段、45–60 分钟真实课程、在线图片、实时音频和人脸数据建立只记路径/大小/时长/摘要的外部 fixture manifest，不把媒体和人脸原图纳入 Git。
- [x] 2.4 实现可重放的 Campaign ID、随机种子、用例到 `task_id` 命名和请求追踪生成器，增加同种子重放测试。
- [x] 2.5 实现负载档位配置，固化 100/300/1000 提交、3/6/12/24/36 长课、1–1000 图片并发、1–150 ASR 会话、50–1000 QPS 和 4/8 小时长稳阶梯。
- [x] 2.6 增加 catalog 完整性聚合测试，确保必需 ID 恰好执行一次，任何失败、缺失、重复或无原始证据都不能聚合为符合。

## 3. A 服务负载生成器基础

- [x] 3.1 先增加负载生成器边界测试，确保业务请求只访问 `control-service:18100` 和 `online-gateway-service:18103`，不允许算子、Redis、Kafka 或 PostgreSQL 作为业务完成捷径。
- [x] 3.2 实现基于异步 HTTP 的并发、限速、阶梯、持续时间、超时、取消和客户端连接池，不无界创建本地协程/socket。
- [x] 3.3 实现结果分类器，区分成功、业务拒绝、限流/过载、超时、连接失败、未定义 5xx、负载机失败和护栏中止。
- [x] 3.4 实现不向普通日志/报告写入 Base64、完整 ASR/OCR 文本、embedding、密码或媒体字节的脱敏证据记录器，增加敏感字段搜索门禁。
- [x] 3.5 实现负载 worker 分片和同一 Campaign ID 聚合，增加分片重复 ID、缺失 worker 和时钟漂移测试。
- [x] 3.6 实现负载主机自检和上限判定，确保本地 CPU/内存/socket/网络先达上限时不把结果归因到被测平台。

## 4. 离线提交、幂等、优先级与查询压力

- [x] 4.1 先增加离线 payload 合同测试，原样使用 `task_id`、`task_types`、`teacher_video_path`、`student_video_path`、`slides_video_path`、`front_points`、`back_point`、`student_count` 和 `asr_options`，不自行改名或增加强制字段。
- [x] 4.2 实现 PPT-only、ASR-only、TEACHER_BEHAVIOR-only、STUDENT_BEHAVIOR-only、PPT+ASR、教师+学生以及四类全量任务发生器。
- [x] 4.3 实现 100/300/1000 唯一短媒体任务突发，记录 HTTP、PostgreSQL、Outbox、Kafka、DAG、队列和最终排空证据。
- [x] 4.4 实现同 `task_id` 的 30/100/300/1000 并发相同提交、冲突媒体提交、分批追加 `task_types` 和已完成结果复用用例。
- [x] 4.5 实现先堆积 100/300 个 `NORMAL`、后注入 10/30 个 `URGENT` 的顺序证据，验证不抢占运行节点。
- [x] 4.6 实现 3/6/12/24/36 真实长课阶梯、阶梯前存储预估、`/data/course` 临时增长和 `/data/result` 持久结果索引。
- [x] 4.7 实现 50/100/300/1000 QPS 查询、2/5 秒带抖动轮询、无抖动惊群和大 ASR 结果查询，验证整数节点状态和结果字段。
- [x] 4.8 增加离线/查询压力的负向比例注入，以可配置 1%/5%/20% 比例混入缺失必需路径、404/超时 URL、未知 `task_types` 和非法区域坐标，并验证同步拒绝、异步失败终态均不污染正常队列；冲突 `task_id` 继续由 4.4 的独立幂等/冲突用例验证。
- [x] 4.9 在离线压力前从 `192.168.29.11` 对 `192.168.29.12:5555` 执行 1/3/10/30 并发媒体下载基线，记录单文件速度、总吞吐、建连耗时、失败率、源端文件服务资源和目标机入站网络，避免把媒体源/网络上限误归因于调度平台或算子。

## 5. 在线图片、实时 ASR 与人脸库压力

- [x] 5.1 先增加在线路径和单图语义测试，覆盖 VBas、FaceRec、ScreenDet、OCR 和 50 MiB 解码上限，确保在线端不执行 RTSP 接入或抽帧。
- [x] 5.2 实现四类在线图片各自 1/3/10/30/60/100/256/512/1000 并发阶梯，记录实例选择、inflight、租约、响应和资源。
- [x] 5.3 实现默认 VBas 60%、FaceRec 15%、ScreenDet 15%、OCR 10% 的可配置混合图片负载。
- [x] 5.4 实现 100/300/1000 路 S 流的 5/10/30 秒单帧请求模型，验证 1000 路、5 秒一帧时约 200 RPS 的持续输入。
- [x] 5.5 实现 Base64 大小、语法、图片格式和解码边界反例，覆盖常规 0.5–5 MiB、49 MiB 和超过 50 MiB，验证拒绝请求不获取租约。
- [x] 5.6 先增加 ASR WebSocket 实时节拍、会话粘性、字幕隔离、中断重连和租约释放测试。
- [x] 5.7 实现 1/10/24/30/60/90/150 实时 ASR 会话阶梯，音频按真实采样速率推送，区分声明容量内和过载结果。
- [x] 5.8 实现 500/1000/5000 人人脸库的新增、批量新增、查询、搜索、删除和识别并发，验证三实例 MongoDB 一致性。
- [x] 5.9 增加 `save_person_photo=false` 全链路证据，扫描容器、MongoDB、日志和持久目录中的人脸原图残留，不在普通报告写入底图。
- [x] 5.10 将里程碑 2B 的 Online Gateway 出站连接池配置为 `max_connections=2048`、`max_keepalive_connections=512` 和有界 `pool_timeout_seconds`，验证 1000 个合法单图并发由租约和算子容量决定结果，不因 Gateway 连接池提前排队/超时产生 `50000`。
- [x] 5.11 将人脸新增、批量新增、查询、搜索和删除固定转发到单一 FaceRec 管理实例，将 `/face/recognize` 通过租约路由到三个识别实例，分别压测并报告单实例管理能力、三实例识别能力和共享 MongoDB 一致性。

## 6. 资源观测、护栏与报告

- [x] 6.1 先增加护栏单元测试，覆盖剩余空间 15%/150 GiB 警戒、10%/100 GiB 红线、GPU Xid/ECC、宿主机 OOM、容器连续重启、数据库不健康和证据无法原子写入。
- [x] 6.2 实现宿主机、Docker、GPU UUID/PID/显存/利用率、Kafka lag、任务队列和容器重启最多每 5 秒一次的常规时序采集；在线图片突发期间每 0.5–1 秒采集实例 `inflight`、活跃租约和容量峰值，并记录 Gateway 请求及租约申请/拒绝/释放累计指标的阶段前后差值。
- [x] 6.3 实现护栏的“停止新负载、保留现场、精确恢复、继续排空”状态机，验证护栏权限高于目标并发。
- [x] 6.4 实现基线、稳定容量、预期过载、负载机限制、护栏中止和真实不符合的分类聚合。
- [x] 6.5 实现 P50/P95/P99、吞吐、成功/拒绝/错误、排队时间、Kafka lag、inflight/租约、资源和恢复时间的中文报告渲染器。
- [x] 6.6 增加相对性能门槛测试：稳定容量内非预期 5xx/连接失败不高于 0.1%、P95 不超过基线 3 倍、P99 不超过 5 倍，过载档不把已定义拒绝误判为系统失败。

## 7. 受控故障注入与恢复

- [x] 7.1 先增加故障注入安全测试，必须要求精确 Compose 身份/完整容器 ID、当前 Campaign 维护锁、恢复动作和超时，禁止宽泛容器匹配。
- [x] 7.2 实现单算子实例停止/TTL/剩余实例接管/原实例恢复用例，覆盖七类算子。
- [x] 7.3 实现单 GPU 上六算子实例组的精确停止与恢复，记录 GPU0/GPU1/GPU2 三组轮换用例。
- [x] 7.4 实现 Control、Orchestrator、Vision Orchestrator 和 Online Gateway 的逐个重启用例，分别验证任务事实、Outbox、DAG、视觉事件、在线请求和 WebSocket 重连。
- [x] 7.5 实现 Kafka 受控重启下的 Outbox 保留、重发、消费恢复和 DAG 幂等用例。
- [x] 7.6 实现 Redis 受控重启下的注册 TTL、新租约拒绝/恢复、旧租约最终回收和不超卖容量用例。
- [x] 7.7 为每类故障注入实现“只有恢复完成才可进入下一故障”的序列门禁，恢复失败时终止相关 Campaign。

## 8. 正式常驻部署与数据库迁移入口

- [x] 8.1 先增加常驻栈入口测试，确保它不复用 Canonical 验收结束时停止新算子实例的恢复语义。
- [x] 8.2 实现 `start-production-stack`，按基础设施、四平台服务、GPU0/GPU1/GPU2/CPU 算子 profile、21/21 注册和就绪顺序启动。
- [x] 8.3 实现 `status-production-stack`，报告四平台、四中间件、21 实例、GPU 归属、注册/租约、关键端口和共享目录状态。
- [x] 8.4 实现 `stop-production-stack`，只停止当前发布权威记录的服务/算子容器，默认不删除容器、镜像、数据卷、`/data/result` 和报告。
- [x] 8.5 增加通用数据库迁移账本和顺序执行器，覆盖 `0001`–`0007` 及后续迁移，验证首次、重复、中断和未知版本。
- [x] 8.6 同步部署 README、单机运维与恢复手册、A 服务对接文档和 Harness，明确常驻入口与 Canonical 验收入口的不同。
- [x] 8.7 为迁移账本引入前已经存在的 PostgreSQL schema 增加严格连续前缀采纳：空库正常迁移，完整旧库匹配唯一 `0001`–`N` 前缀后入账并执行剩余迁移，部分结构、数据不变量或漂移失败关闭。

## 9. 旧容器与旧镜像精确清理

- [x] 9.1 先增加伪造 inventory 测试，覆盖运行容器、已停止容器、多 tag 同 ID、RepoDigest、悬空镜像、基础镜像、当前/旧 release 和部分 inspect 失败。
- [x] 9.2 实现容器/镜像库存和保护集生成器，保护运行容器引用、当前 11 镜像、回滚基线、基础镜像和显式 allowlist。
- [x] 9.3 实现候选集差集、删除原因、release 归属、标签/digest 和预估回收空间，保护集与候选集交集非空时失败关闭。
- [x] 9.4 实现原子 dry-run 计划、执行前二次 inspect 和状态漂移拒绝，删除命令只接受计划中完整容器/镜像 ID。
- [x] 9.5 增加禁止 `docker system prune -a`、`docker compose down -v`、volume、`/data/result`、模型、Git 和 release 证据目标的静态与运行时门禁。
- [x] 9.6 实现构建前清理阶段，只删除不影响当前可运行/回滚基线的精确旧镜像 ID，记录 `docker system df` 前后证据。
- [x] 9.7 实现新版验收后退役阶段，先保存旧容器 inspect/digest/release 证据，再按完整 ID 删除旧容器、重算候选并删除旧镜像。
- [x] 9.8 实现清理后的基础设施、四平台、21实例、GPU、注册/租约和 7/7 Smoke 重验，任一不就绪都不得发布清理完成结论。

## 10. 本地分层验证与 OpenSpec/Harness 一致性

- [x] 10.1 在平台 `.venv` 执行新增 Campaign、部署、迁移和清理代码的 Ruff、strict Mypy、`compileall` 和导入测试。
- [x] 10.2 执行 catalog、负载分类、基线门槛、护栏、故障注入、报告聚合和镜像差集的全部单元测试。
- [x] 10.3 使用真实 PostgreSQL、Redis 和 Kafka 执行任务突发、幂等、Outbox、DAG、重启恢复和数据库迁移账本集成测试。
- [ ] 10.4 启动四服务和可用本地算子执行缩小版 Campaign，验证北向路由、HTTP/WS、资源采集、护栏与原子报告。
- [x] 10.5 对 Compose、预检、常驻入口、精确清理和报告路径执行 shell/static 测试，证明无宽泛 prune、无数据卷删除、无 Text Analysis 当前拓扑。
- [x] 10.6 更新 Harness 场景、verification、change ledger、部署 README 和总体设计，保留旧架构图和历史 release 原文。
- [x] 10.7 运行 `openspec validate run-milestone-2b-extreme-load-campaign --strict`、受影响活动变更严格校验和 `git diff --check`，记录实际达到的验证层级。
- [x] 10.8 创建 `algorithm-scheduling-platform/deploy/算法功能调度平台部署手册.md`，记录七算子、四平台、四中间件、三 GPU、21 实例、端口、网络和数据边界。
- [x] 10.9 补全手册的前置检查、Git/镜像/模型准备、11 个 `config.toml`、目录权限、数据库迁移和首次部署顺序，明确当前不使用 `.env`，并记录 Online Gateway 的 `2048/512` 连接池配置、有界池等待以及媒体源 1/3/10/30 并发下载基线命令和判定方法。
- [x] 10.10 补全手册的 `start-production-stack`/`status-production-stack`/`stop-production-stack`、A 服务离线/在线 Smoke、升级、回滚、构建前清理和验收后精确退役。
- [x] 10.11 补全手册的日志、数据留存、备份边界、健康/注册/租约/Kafka lag/GPU/共享目录排障和部署前后可勾选清单，明确 FaceRec 管理接口固定单实例、识别接口租约路由三实例的运行边界。
- [x] 10.12 实现部署手册静态校验，解析脚本路径、Compose 服务名、配置路径、端口和禁止操作，确保手册不复制第二套可漂移部署逻辑。
- [x] 10.13 更新平台 README、部署 README 和单机运维手册指向同一份中文部署手册，删减相互冲突的重复操作步骤。
- [x] 10.14 增加固定四任务查询的终态轮询回归并修复未请求 `status=0` 永久阻塞单任务泳道的问题，覆盖单项/多项成功、非终态等待、全未请求和失败终态。
- [x] 10.15 保留 `2154c40` 阶段 0 诊断 attempt，区分 PPT 正常 EOF 误判、无有效语音 fixture 和视觉消费循环退出三类原因，不覆盖已有 case 与 runtime metric。
- [x] 10.16 修复 PPT 正常 EOF/最小帧阈值、视觉普通单任务错误终态/基础设施错误隔离、滞后进度竞态，并在 Git 工作区外冻结同一课程 50 秒同步 T/S/P 新 fixture，通过北向 ASR-only 证明教师片段可识别，完成算子和平台聚焦回归。
- [x] 10.17 保留 `7efb2a0` 阶段 1 的瞬时 Kafka/Control 指标阻断证据，将 Kafka lag 采集收敛为单次 all-groups 快照、三个必需消费组和同一采样内有限重试，验证持续失败仍锁存 STOP。
- [x] 10.18 将 Kafka lag 从 Control 中拆为独立采集面，使用独立 20 秒超时和最多一次重试，原子发布脱敏失败事件并由用例结果引用，验证最终恢复不覆盖已锁存 STOP。
- [x] 10.19 保留 `28e74d7` attempt 只完成 `BASE-MEDIA-DOWNLOAD-1/3/10` 的预执行诊断证据；修复 Fault Adapter 对 attempt root 的严格识别，并通过专用 `_LocalCampaignLockGuard` 持有当前 attempt 根下绑定 Campaign/attempt 的 `0600` 单链接 `.campaign-fault.lock`，与 `.11` delegated canonical 锁分离，验证故障 case 全程持锁、逐动作本地探针、远端 challenge 和非法绑定失败关闭。
- [x] 10.20 保留 `4dc40757` attempt 在 `OFF-UNIQUE-PPT-100` 的第 33 份运行时 `STOP` 证据；修复 PPT 终态回调与对账并发命中同一节点时的同终态幂等竞态，验证冲突终态仍失败关闭，不补写中断 case。
- [x] 10.21 保留 `da1f5e37` attempt 在 `OFF-UNIQUE-ASR-300` 已提交但缺少规范终态和退出码的执行器中断证据，并记录已接受任务自然排空；阶段 0/3 与阶段 4/6 的实时 ASR 成功必须收到 `finished=true`，并为下一 attempt 固化脱离交互终端的持久 runner、PID、日志和逐案退出码边界。

## 11. `192.168.29.11` 七算子四平台发布

- [x] 11.1 在目标机获取同一最终 Git SHA，校验 clean clone、模型 manifest、三 GPU、NVIDIA Runtime、端口、基础设施、共享目录、评审输入和当前数据库迁移，并对媒体源执行 1/3/10/30 并发下载基线而不只检查 URL 可达性。
- [x] 11.2 发布构建前镜像清理 dry-run，核对保护集/候选集后按完整 ID 执行第一阶段清理；若 BuildKit 仍持有可回收层且磁盘未越过警戒线，只能在本次明确授权下执行 `docker buildx prune --all --force --keep-storage 100GB`，记录前后证据并确认剩余空间高于警戒线。
- [x] 11.3 构建并 inspect 七个算子和四个平台镜像，验证 `amd64`、完整 revision label、模型资产、日志目录和精确镜像 ID，不构建 Text Analysis。
- [x] 11.4 使用常驻入口启动 PostgreSQL、Kafka、Redis、MongoDB、四平台和 21 个算子实例，证明 21/21 注册、18/18 GPU 真实进程、3/3 CPU PPT 和 7/7 Smoke。
- [x] 11.5 对 A 服务只开放 `18100` 和 `18103`，重验中间件、内部服务和21个算子宿主机端口只绑定 `127.0.0.1`。
- [x] 11.6 发布远端预检、容器/镜像库存、配置权威、GPU PID/cgroup、注册、租约和 Smoke 原始证据。
- [x] 11.7 由未依赖隐式开发上下文的执行者按部署手册在 `192.168.29.11` 复现预检、常驻启动、状态查询和 A 服务 Smoke，记录口头补充、命令漂移或缺失步骤并修正手册。
- [x] 11.8 将阶段 0 修复形成并推送为新完整 Git SHA，在 `192.168.29.11` 重新构建并逐 ID inspect 全部七算子和四平台镜像，恢复同 revision 的 29/29 healthy、21/21 注册、18 GPU、3 CPU PPT 与 7/7 Smoke；11.3–11.7 的旧 SHA 证据只保留为历史发布事实，不得直接授权 12.1。
- [x] 11.9 将 PPT 终态并发幂等修复形成新完整 Git SHA，重建并 inspect 11 个同 revision 镜像，恢复 29/29 healthy、21/21 注册、18 GPU、3 CPU PPT 与 7/7 Smoke；使用新 seed、Campaign ID 和 write-once attempt 从阶段 0 重跑，不续写 `4dc40757` 中断 attempt。
- [ ] 11.10 将实时 ASR 终态门禁和持久 runner 约束形成新完整 Git SHA，重建并 inspect 11 个同 revision 镜像，恢复完整拓扑；使用新 seed、Campaign ID 和 write-once attempt 从阶段 0 重跑，不续写 `da1f5e37` 中断 attempt。

## 12. 远端极限负载 Campaign 执行

- [ ] 12.1 执行阶段0的单请求与单泳道基线，建立各接口/算子的延迟、吞吐、实时率、资源和结果结构基线。
- [ ] 12.2 执行阶段1的 PPT、ASR、教师行为、学生行为单泳道及 3/6/12/24/36 长课阶梯，每级达到警戒线时停止升级。
- [ ] 12.3 执行阶段2的 100/300/1000 唯一提交、幂等竞争、追加任务类型、NORMAL/URGENT 插队、负向比例和 50/100/300/1000 QPS 查询。
- [ ] 12.4 执行阶段3的四类图片 1–1000 并发、100/300/1000 路 S 流、ASR 1–150 会话、图片边界反例和 500/1000/5000 人人脸库用例；人脸管理请求固定单实例、识别请求租约路由三实例，分别给出容量结论。
- [ ] 12.5 执行阶段4的日常、高峰和极限混合档，记录每档稳定容量、预期过载、队列排空和恢复时间。
- [ ] 12.6 执行阶段5的七类单实例、三组单 GPU、四平台服务、Kafka 和 Redis 故障注入，每次恢复原状后再继续。
- [ ] 12.7 执行阶段6的稳定容量 70%–80% 至少 4 小时长稳；资源允许时执行 8 小时扩展长稳。
- [ ] 12.8 停止新负载后等待已接受任务进入预期终态、Kafka lag 和节点队列可证明持续排空、租约最终释放，记录排空上限和剩余异常。

## 13. 最终验收、清理与交接

- [ ] 13.1 在同一最终 SHA 下重跑当前权威的 217 条反例、26 条压力/恢复用例和 6 项 B 级人工复核，不用新 Campaign 代替原有门禁。
- [ ] 13.2 聚合预检、基线、单泳道、突发、在线、混合、恢复、长稳和原有门禁，任何失败或必需阶段未执行都不得发布“全部符合”。
- [ ] 13.3 如 ASR-013 或其他 B 级质量项仍失败，保留完整性能/恢复结果，但将里程碑最终结论明确标记为质量阻断而非全部通过。
- [ ] 13.4 新版所有必需门禁符合后，发布验收后旧容器/旧镜像 dry-run，审核后按完整 ID 删除并记录回收空间。
- [ ] 13.5 清理后重新验证基础设施、四平台、21实例、18 GPU、3 CPU、注册/租约、7/7 Smoke、`/data/result` 和历史报告仍然完整。
- [ ] 13.6 更新 Harness change ledger 的最终 SHA、远端 release、用例数、容量结论、故障恢复、清理证据和剩余风险，不回写历史 release。
- [ ] 13.7 运行全部 OpenSpec 严格校验、Harness 一致性、报告摘要和 `git diff --check`，使用中文 Conventional Commit 提交并推送当前变更分支。
- [ ] 13.8 将部署手册的适用拓扑版本、配置 schema、最后验证日期和实际最终 Git SHA/release 证据补齐，确认手册不引用旧八算子 release 冒充当前验收。
