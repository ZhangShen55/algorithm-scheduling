## 1. 现场基线与实现边界

- [x] 1.1 固化 2026-09-03 混合压力失败基线，引用 Run ID、116 次在线业务失败、两项 Vision 租约连接失败、Control 替换时间和现有 Harness，禁止覆盖历史证据
- [x] 1.2 读取 Vision、Online Gateway、Control 租约接口、共享租约恢复工具和 VBas 在线准入实现，形成可恢复与确定性错误矩阵
- [x] 1.3 核对三个 VBas 的 `online=24`、`offline=1` 和内部 `24` 队列边界，增加测试证明 Control 每实例最多发放 24 个在线租约
- [x] 1.4 确认本变更不修改接口路径、请求字段、成功响应、四服务边界和 VBas 模型推理逻辑

## 2. 先补失败测试

- [x] 2.1 为 Vision 增加 Control 连接失败后恢复、502/503/504 后恢复、容量不足等待和预算耗尽测试，并先证明旧实现失败
- [x] 2.2 为 Vision 增加确定性 4xx、非法租约响应、稳定 `batch_id`、节点 attempt 不增长和最终失败事件测试
- [x] 2.3 为 Online Gateway 增加统一 deadline 测试，覆盖容量等待加 VBas 重试共享预算、上游取消和租约最终释放
- [x] 2.4 为 Online Gateway 增加 VBas 建连失败、连接复位、429/502/503/504、读取超时、实例重选和有限次数测试
- [x] 2.5 为 Online Gateway 增加图片或坐标参数错误不重试，以及 `50301`、`50302`、`50201`、`50401`、`50000` 精确映射测试
- [x] 2.6 为结构化日志和指标增加脱敏测试，证明不记录 Base64、完整请求/响应和算法结果

## 3. 共享错误分类与退避

- [x] 3.1 在合适的共享包中实现容量不足、Control 瞬时失败、确定性失败和非法响应的类型化异常，避免依赖异常文本判断
- [x] 3.2 实现可接收 monotonic deadline 的指数退避与随机抖动工具，默认从 0.2 秒增加并在 2 秒封顶
- [x] 3.3 保持既有租约续租和释放恢复行为，补充申请、续租、释放异常之间不会相互覆盖根因的测试

## 4. Vision 租约恢复

- [x] 4.1 修改 Vision 容量客户端，使容量不足、连接错误、连接超时和 502/503/504 在累计 300 秒预算内重试
- [x] 4.2 保证未取得租约不调用 VBas，重试期间节点保持状态 50，取得租约后继续使用原任务、帧摘要和稳定 `batch_id`
- [x] 4.3 保证确定性错误或预算耗尽时写入非空中文原因，并可靠发布视觉失败终态事件供 Orchestrator 清理媒体
- [x] 4.4 增加 Vision 运行时关闭和 Kafka 重放测试，验证取消等待、释放租约且不产生重复逻辑工作

## 5. Online Gateway 有界等待与调用恢复

- [x] 5.1 将现有 `hard_timeout_seconds=600` 落实为整个在线请求的单一总预算，容量等待和每次算子调用均按剩余时间执行
- [x] 5.2 使 Control 容量不足和可恢复连接错误在剩余预算内退避等待，确定性错误快速失败
- [x] 5.3 为人数、教师和学生在线纯推理实现默认首次加两次有限尝试，每次失败释放旧租约并重新申请实例
- [x] 5.4 对 VBas 429/502/503/504、建连或协议异常和读取超时执行允许的重试，同时保持相同 `TaskID`、`ImageID` 和请求数据
- [x] 5.5 传播 A 服务取消，及时终止容量等待或下游调用并释放租约，验证活动租约和后台任务均归零
- [x] 5.6 实现最终业务码 `50301`、`50302`、`50201`、`50401` 和 `50000` 的精确语义，保留 HTTP 200 和响应字段结构

## 6. 配置、日志、指标与文档

- [x] 6.1 为 Vision 和 Online Gateway 的相关 `config.toml` 补充中文注释、范围校验和有效配置输出，避免单次 read timeout 提前绕过总预算
- [x] 6.2 增加按 pool、capability、instance、stage、exception_type 和 outcome 统计的等待、重试、重选、超时、失败与释放指标
- [x] 6.3 增加包含 `trace_id`、attempt、已耗时和剩余预算的脱敏结构化日志，确保最终失败能够完整归因
- [x] 6.4 更新 Online Gateway、Vision README、平台部署手册和 A 服务对接指南，说明 600 秒平台预算、建议 A 超时 630 至 660 秒及新增错误码
- [x] 6.5 新增中文 Harness 计划，定义服务器发布、运行事实锁定、压力矩阵、GPU 和文件生命周期证据路径

## 7. 本地实现验证

- [x] 7.1 在平台 `.venv` 中运行两个服务的新增聚焦测试并确认全部通过
- [x] 7.2 运行 `vision_orchestrator_service` 和 `online_gateway_service` 全量测试、相关跨服务容量测试及平台非集成回归
- [x] 7.3 对变更 Python 文件运行 Ruff、严格类型检查、`compileall` 和 `app.main:app` 导入验证
- [x] 7.4 本地启动两个服务并检查 health/readiness、在线真实图片 Smoke、日志脱敏和错误码响应
- [x] 7.5 运行 OpenSpec strict 校验、Harness 相关测试和 `git diff --check`，记录所有测试数量与非本变更跳过项

## 8. 服务器发布准备与缓存构建

- [ ] 8.1 在 `192.168.29.11` 记录目标完整 Git SHA、受影响服务旧容器/镜像完整 ID、revision、配置 SHA-256、健康状态和磁盘空间
- [ ] 8.2 创建独立干净 release 源码目录，确认服务器三张 GPU、共享目录、中间件、媒体源和负载机可用
- [ ] 8.3 使用现有 BuildKit 缓存构建所有实际受影响镜像，不使用 `--no-cache`，并逐个校验 amd64、revision、源码 manifest、compile 和 import
- [ ] 8.4 全部新镜像通过校验后逐个替换目标服务，新容器完成 health、readiness、注册和真实 Smoke 前保留旧镜像与回滚信息
- [ ] 8.5 新容器通过后精确删除被替换的旧容器和无引用旧镜像，确认构建缓存、无关镜像、数据卷和历史 Harness 证据仍存在

## 9. 运行事实锁定与 GPU 基线

- [ ] 9.1 为每个 attempt 生成唯一 Run ID，冻结容器 ID、镜像 revision、配置摘要和媒体输入，并在测试期间周期复核
- [ ] 9.2 每组 VBas 正式压力前重新创建三个实例，等待 ready 和 5 分钟空闲稳定，记录最后 60 秒显存中位数为冷启动基线
- [ ] 9.3 启动每 2 秒资源采样，记录三卡显存、利用率、功耗、PID 到容器完整 ID、在线/离线租约和 VBas running/queued
- [ ] 9.4 修改压力驱动，保存全部失败分类计数，直接采集 active lease，并使用 `df` 加本轮目录增量替代高频全树 `du`
- [ ] 9.5 增加运行事实变化门禁；容器 ID、revision 或配置摘要变化时将 attempt 标记环境失效且不得判为通过

## 10. Control 故障注入验收

- [ ] 10.1 在视觉任务已有真实 VBas 批次后执行 Control 不可用 5 秒测试，等待课程、租约、Kafka、Outbox 和缓存目录全部收敛
- [ ] 10.2 使用全新 task_id 分别执行 Control 不可用 15 秒和 30 秒测试，每轮均等待完整终态
- [ ] 10.3 核对三轮视觉节点全部成功、attempt 为 1、逻辑 batch 不重复、错误原因无空文本且媒体正确清理
- [ ] 10.4 任一故障注入失败时冻结证据、定位修复并从该档新建 attempt 重跑，不得继续给出恢复能力通过结论

## 11. 在线分档与重复压力

- [ ] 11.1 对人数、教师和学生三条 VBas 在线路由分别执行可恢复故障注入与真实图片回归，验证响应兼容、实例重选和租约释放一致
- [ ] 11.2 执行在线人数识别 72 并发、5000 次，统计 HTTP、业务码、延迟、吞吐、实例分布、租约和 GPU
- [ ] 11.3 执行在线人数识别 144 并发、10000 次，并等待响应、租约和 VBas 队列完整收敛
- [ ] 11.4 使用独立 Run ID 连续执行至少三轮 200 并发、10000 次，每轮均等待终态和 5 分钟显存恢复观察
- [ ] 11.5 验证每轮 HTTP 与业务成功率 100%，所有平台错误码和连接异常为 0，三个 VBas 均接受真实请求且无固定实例独占
- [ ] 11.6 任一请求失败时保存全量分类和关联证据，停止宣称在线通过并完成根因修复后从失败档重新执行

## 12. 离线全量与最终混合压力

- [ ] 12.1 独立执行一轮 20 路 `PPT`、`ASR`、`TEACHER_BEHAVIOR`、`STUDENT_BEHAVIOR` 全量任务并等待全部终态
- [ ] 12.2 验证 80 个任务类型全部成功、全部节点 attempt 为 1，无状态冲突、批次重复、空失败原因和消费者 unhealthy
- [ ] 12.3 使用独立 Run ID 同时开始 20 路全量任务与在线人数识别 200 并发、10000 次，完整执行第一轮混合压力
- [ ] 12.4 第一轮全部收敛后重新建立 VBas 冷启动基线，使用全新 task_id 完整执行第二轮同规模混合压力
- [ ] 12.5 验证两轮均为在线 100% 业务成功、80 个任务类型成功终态、三台 VBas 有在线和离线真实工作且无租约超卖
- [ ] 12.6 核对每轮最终活动租约、reported inflight、VBas running/queued、三个 Kafka Consumer lag、未发布 Outbox 均为 0

## 13. 显存异常调查门禁

- [ ] 13.1 每轮终态后继续观察 5 分钟，计算最后 60 秒恢复中位数并与本轮冷启动基线及前轮恢复值比较
- [ ] 13.2 若任一卡恢复差值超过 512 MiB 或连续轮次单调增长，立即暂停后续负载并按接口、pool、阶段、实例、租约、队列和模型内存生命周期归因
- [ ] 13.3 若发生 OOM、GPU Xid、容器重启或剩余显存低于 2 GiB，立即停止驱动、保存进程与容器现场并将该轮判为失败
- [ ] 13.4 显存异常修复后重新构建受影响镜像并从冷启动重新执行失败轮次，禁止沿用部分结果或直接降低验收标准

## 14. 文件生命周期与最终报告

- [ ] 14.1 在离线和混合轮次持续验证 `slides.mp4`、`teacher.wav`、`teacher.mp4`、`student.mp4` 及视觉临时目录按实际消费者终态删除
- [ ] 14.2 验证每个终态课程的 `/data/course/{task_id}` 消失，`/data/result/{task_id}`、结构化数据库结果和失败事实完整保留
- [ ] 14.3 为每轮保存 write-once 原始报告，并形成中文 Harness，包含环境、请求规模、全量错误分类、任务终态、三卡分布、GPU、存储和结论
- [ ] 14.4 只有全部必需轮次、显存恢复和收敛门禁通过后才给出变更通过结论；任何未完成、中断或环境失效必须明确列出且不得包装为通过
- [ ] 14.5 最终复查服务器健康、注册、租约、Kafka、Outbox、共享目录和结果可查询性，规范提交并推送实现、文档、OpenSpec 与 Harness 记录
