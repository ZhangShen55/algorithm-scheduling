## 1. 固化诊断基线

- [x] 1.1 将 `offline20-20260904T024034Z` 的原始配置、节点时间线、Kafka lag、ffmpeg/VBas 活跃情况和受控停止原因整理到独立中文 Harness，明确该轮未完成且不得标记为通过
- [x] 1.2 将 `maxpoll16-20260904T033113Z` 的临时配置、任务状态、首批 VBas 延迟和媒体队列独占现象整理到同一 Harness，区分“Consumer 预取改善”和“端到端吞吐仍未解决”
- [x] 1.3 停止诊断任务后仅清理对应 task_id 的数据库事实、Kafka 未提交测试消息和 `/data/course`、`/data/result` 缓存，保留受限报告目录

## 2. 先补视觉 Consumer 回归测试

- [x] 2.1 新增错峰到达测试：第一条慢命令运行时，后续命令持续填充到 `worker.concurrency`
- [x] 2.2 新增同 poll 快慢命令测试：快命令完成后立即补位，不等待慢命令
- [x] 2.3 新增积压量超过上限测试，证明执行中和 pending 消息数量均有界
- [x] 2.4 新增跨 poll 乱序完成测试，证明同 partition 只提交连续完成 offset
- [x] 2.5 新增停止、取消和重启测试，证明未完成消息不提交且能够安全重放

## 3. 实现持续补位的命令运行时

- [x] 3.1 将 `VisualCommandConsumerLoop` 的在途任务、pending 消息和 partition 完成水位提升为跨 poll 生命周期状态
- [x] 3.2 按空闲槽位持续 poll 和启动命令，使 `kafka.max_poll_records` 不再成为封闭批次的有效并发上限
- [x] 3.3 实现 pending 有界缓存、较高 offset 完成登记和连续水位提交
- [x] 3.4 实现停止领取、优雅收敛、超时取消与 rebalance 清理，确保未完成 offset 可重放
- [x] 3.5 运行 Consumer 单元测试并确认旧的非法信封、旧命令代次和状态冲突回归用例仍通过

## 4. 先补媒体公平调度与取消测试

- [x] 4.1 新增长视频与短视频并发测试，证明短视频 ffprobe 和首批抽帧不会等待长视频全部时间点
- [x] 4.2 新增多课程压力单元测试，断言媒体等待作业数不超过推导上限且 ffmpeg 进程不超过 `media.max_concurrent_processes`
- [x] 4.3 新增确定性 `FrameBatchPlan` 测试，覆盖重复时间点、尾部不足一批、乱序完成和重放
- [x] 4.4 新增失败与取消测试，证明本命令的 pending 作业和子进程被清理且不影响其他课程

## 5. 实现有界公平媒体调度

- [x] 5.1 引入按排序时间点和 `scan.batch_size` 生成的确定性 `FrameBatchPlan`
- [x] 5.2 将一次性为全部时间点创建协程的实现改为每命令有限 batch 预取和跨命令轮转调度
- [x] 5.3 为 ffprobe 提供不会被抽帧队列饿死的执行入口，同时保持全局进程硬上限
- [x] 5.4 使用可取消的 asyncio 子进程执行 ffmpeg/ffprobe，并在超时、取消和关闭时终止及回收子进程
- [x] 5.5 增加媒体 pending、running、首批等待和子进程清理指标，日志只记录脱敏任务与 batch 上下文

## 6. 先补抽帧与推理流水线测试

- [x] 6.1 新增首批图片准备完成即调用 VBas 的测试，证明无需等待整轮抽帧
- [x] 6.2 新增抽取下一批与推理当前批重叠的并发测试，并断言生产者/消费者队列有界
- [x] 6.3 新增尾部不足 `scan.batch_size` 仍完整返回结果的测试
- [x] 6.4 新增相同命令重试时 `image_id`、`batch_id`、帧顺序稳定的测试
- [x] 6.5 新增学生全画面、前排、后排共享帧文件测试，断言每个时间点只执行一次有效视频解码
- [x] 6.6 新增单命令某批失败的隔离测试，断言租约、媒体生产者和未开始批次清理且其他命令继续

## 7. 实现视觉分析流水线

- [x] 7.1 在命令生命周期内建立容量为两个 batch 的媒体生产者与推理消费者队列
- [x] 7.2 将教师粗扫和细化扫描改为确定性 batch 流式推理，同时保持现有候选窗口与区间聚合语义
- [x] 7.3 将学生全画面、前排和后排分析改为单次抽帧资产加区域推理，保持现有结果结构和兜底规则
- [x] 7.4 使 VBas Client 接受并逐批返回结果，继续使用 Control 离线租约、稳定身份、有限重试和动态容量门控
- [x] 7.5 实现命令级结构化取消和 `finally` 清理，确保失败终态事件、租约释放和媒体生命周期完整

## 8. 细化任务状态与可观测性

- [x] 8.1 将 Orchestrator 发布视觉命令后的原因改为“视觉命令已发布，等待 Vision 消费”，不提前声明已领取
- [x] 8.2 在 Vision 消费、视频校验、时长探测、抽帧、容量等待、推理、聚合和持久化阶段写入对应中文原因
- [x] 8.3 为抽帧和推理写入去抖的已完成 batch/总 batch 进度，避免逐帧更新数据库
- [x] 8.4 增加视觉命令 pending/in-flight、槽位利用率、媒体队列、首个 VBas 请求延迟和 batch 进度指标
- [x] 8.5 补充任务查询与运维接口回归测试，确认整数状态码、字段结构和 A 服务契约不变

## 9. 配置、文档与本地验证

- [x] 9.1 更新 `vision_orchestrator_service/config.toml` 注释，明确 `worker.concurrency`、`kafka.max_poll_records`、媒体进程上限和 batch 预取语义
- [x] 9.2 更新 Vision README、部署配置和相关设计文档，说明持续补位、流水线、状态阶段与资源上限
- [x] 9.3 完成 `vision_orchestrator_service` 的 `python -m compileall -q app` 和 `from app.main import app` 验证
- [x] 9.4 运行 Vision 全量测试、Orchestrator 受影响测试、公共包测试和跨服务集成测试，保存精确命令与结果
- [x] 9.5 记录中文 Harness，包含失败先行用例、修复后证据、未执行的服务器业务压力测试和剩余风险

## 10. 服务器缓存构建与容器替换

- [ ] 10.1 记录 `192.168.29.11` 受影响容器 ID、镜像 ID、Git revision、配置摘要、健康状态和精确回滚命令
- [ ] 10.2 使用现有 BuildKit 缓存构建 `vision-orchestrator-service`，并在共享包或 Orchestrator 代码受影响时一并构建 `orchestrator-service`；不得使用 `--no-cache` 或全局 prune
- [ ] 10.3 校验新镜像 revision、`linux/amd64` 架构、`app.main:app` 导入和配置挂载路径后逐个替换容器
- [ ] 10.4 仅执行 health、readiness、Kafka Consumer 和运行版本门禁，不代替用户发起最终业务压力测试
- [ ] 10.5 新容器全部正常后精确删除被替换的旧容器和旧镜像；失败时回滚并保留失败证据
- [ ] 10.6 更新 Harness 的部署资产、门禁结果、删除清单和“最终压力测试待用户执行”结论
